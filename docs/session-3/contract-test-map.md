# Session 3 — Edge-Facing Contract Test Map

> **The session's progress bar.** One row per edge-facing contract clause → named test(s) → status.
> Gate-3 greps the `CONTRACT-MAP-SUMMARY:` line at the end of this file; **regenerate it on every edit**.
> The session ends when every row is PASSING or ADR-renegotiated. Silent under-delivery is the one
> unforgivable outcome.
>
> Sources cited per row: `docs/session-3/charter.md` (the verbatim Session-3 charter — this map is its
> §3 V3 deliverable), `docs/consistency-contract.md` (§/line), `docs/architecture.md` (§/line),
> ADR-0034/0035/0036/0037/0038/0039, the S2→S3 handoff (`docs/session-2/handoff-to-session-3.md`).
> Every PASSING claim was verified by **reading the test body**, not from memory
> (see `contract-test-map-notes.md` §(c) for what each existing test actually asserts; for the
> C1 rows, `docs/session-3/reviews/c1-contract-qa-audit.md` — the post-C1 row-by-row audit; for
> the C2 rows, `docs/session-3/reviews/c2-contract-qa-audit.md` — the post-C2 row-by-row audit).

## Status legend

| Status | Meaning |
|---|---|
| `PASSING` | A real test exists at the clause's **proper level** and asserts the guarantee. Exact test named. |
| `PARTIAL(unit)` | Genuine class-level/in-process coverage exists, but the guarantee is **not** demonstrated at the required level (edge process / wire / sim with real propagation). Missing level named in Notes. |
| `FAILING-CAPTURED` | Test exists, fails, failure captured, `@Disabled` with an `S3-BACKLOG` marker. |
| `UNIMPLEMENTED` | No test exists for the clause (the planned test name is given). |
| `ADR-RENEGOTIATED(adr-ref)` | Clause renegotiated by an accepted ADR; row closed by reference. |
| `N-A(reason)` | Clause not applicable as written (reason given). |

## Level legend

- **UNIT** — class in isolation (JUnit, single JVM, no wiring).
- **BOUNDARY** — the in-process ADR-0034 interface (`readSince`/`ReplaySource`) — this *is* the proper level for ADR-0034's own clauses.
- **SIM** — deterministic simulation over the real `RaftNode`/`ConfigStateMachine` (configd-testkit harness).
- **PROCESS** — separately running edge process over the real wire (C2 transport, mTLS), or the C1 fan-out service endpoint.
- **GATE** — a gate-3 mechanical check (JMH `-prof gc`, jcstress, static-guard test, metrics presence).

## Component legend (charter §4, `docs/session-3/charter.md` — verbatim component scopes)

- **C1 — Fan-out distribution service** (charter:104-112): consumes the ADR-0034 boundary; push-based
  cursor-acknowledged streams per subscriber; §7 subscription model; the coalescing/gap rule ("define and
  test the exact rule"); §11 backpressure — bounded per-subscriber queues, explicit overflow→catch-up
  demotion, every policy threshold a named config with a metric.
- **C2 — Edge node process** (charter:114-124): separately runnable process; transport + mTLS (ADR-0037
  deviation recorded in the charter itself, :117); versioned snapshots + delta application onto the
  lock-free read path (hot-path law inherited, brief JMH gc-profile); every read returns its cursor;
  per-session monotonic reads incl. across reconnect to a *different* fan-out endpoint; the staleness
  commit-timestamp clock + `X-Configd-Stale` serving surface live here (delta-apply/serving path).
- **C3 — Catch-up, replay, gap detection** (charter:126-131): cursor-gap detection on the edge; replay
  from the boundary or snapshot+delta re-bootstrap past the replay horizon (horizon-boundary case under
  concurrent writes); poison-pill + negative caching — implement or explicitly descope by ADR.
- **C4 — Slow-consumer policy** (charter:133-136): §7 thresholds (demotion to catch-up, quarantine,
  disconnect, re-bootstrap); each transition: a test, a metric, a structured log; sim-exercised.
- **C5 — New-edge bootstrap** (charter:138-141): zero-state join under sustained concurrent writes; no
  gap, no duplicate-application divergence; judged by the V1 snapshot–delta equivalence invariant.
- **C6 — End-to-end integration** (charter:143-149): Compose 3 control-plane + ≥3 edge processes;
  scripted scenarios (sustained writes, leader kill mid-stream, edge partition→demotion→convergence,
  fresh-edge bootstrap mid-load); RR-095 stall-seed re-run.
- **G3** — cross-cutting gate-3 mechanical rows (charter §5: JMH gc-profile check, metrics presence)
  that no single component owns.

---

| Row | Contract clause (doc §, verbatim guarantee) | Level required | Named test(s) | Component | Status | Notes |
|---|---|---|---|---|---|---|
| CT-01 | §2 Staleness Measurement (contract:63): "staleness = `edge_wall_now − commit_timestamp(last_applied_notification)` … assigned by the leader at commit/apply time"; ADR-0035 handoff item 2: make the ignored `timestamp` param of `StalenessTracker.recordUpdate` load-bearing | UNIT + SIM | `StalenessTrackerTest` (reworked: `$StalenessMeasurement#stalenessUsesCommitTimestampNotRecordTime`, `$HeartbeatFrontier`), `EdgeClientCoreTest` (`$NotifyHandling#appliedNotifyAdvancesFrontierToCommitTimestamp`, `$HeartbeatFrontier`, `$SnapshotFrontierIntegrity`), `EdgeStalenessFrontierSimTest` (SIM) | C2 | PASSING | **FLIPPED at the C2 audit (2026-06-11, bodies read; fresh surefire 23:03–23:05, all green):** ADR-0039 is in the code — `StalenessTracker.recordUpdate(version, commitTimestampMillis)` makes the leader commit timestamp load-bearing (`staleness = wall_now − frontier`; data age, not record time — pinned exactly by `stalenessUsesCommitTimestampNotRecordTime`); the idle-time proxy is **deleted**, not kept alongside. Frontier = max(commitTs(last applied), serverNow(cursor-matched HEARTBEAT)); `latestSeq > cursor` never advances it (real lag stays visible). SIM half drives the production `EdgeClientCore` via `C1StreamDriver`: idle-but-heartbeating edge pinned CURRENT across ≥35s; partitioned edge walks the full ladder. The originally-owed `StalenessTrackerCommitTimestampTest` landed as this rework (cite what exists). Snapshot cutover records version WITHOUT a frontier advance (ADR-0028 bodies carry no commit ts — part-(a) defect found+fixed in C2(b); `$SnapshotFrontierIntegrity`). Producer half was CT-21 (PASSING since C1). |
| CT-02 | §2 INV-S2 (contract:82-84): "P(staleness > 500ms) < 0.01 (p99); P(staleness > 2s) < 0.0001 (p9999)"; §7 row (contract:214): "The p99-distribution test (INV-S2) is owed to Session 3" | SIM (mechanism) | `EdgeStalenessDistributionSimTest` (SIM mechanism: `PropagationProbe` publish→visible samples through the production C2 path), rebuilt `ConsistencyPropertyTests$StalenessUpperBoundTest` (frontier-driven); owed (Session 5 / V2): the real p99 < 500 ms distribution over real propagation latency | C2 (+V2 probe) | PARTIAL(unit) | **C2 audit 2026-06-11 (bodies read):** the *mechanism* half is now real at SIM level — `EdgeStalenessDistributionSimTest` feeds a `PropagationProbe` publish→visible samples through `C1StreamDriver`/production `EdgeClientCore` (CT-01's clock + ADR-0039 frontier), asserting a non-vacuous, monotone (p50 ≤ p99 ≤ max) distribution and no stuck-DISCONNECTED edges under a no-fault schedule, deliberately with NO p99 target (charter V2: sim logical time proves the *mechanism*; "Session 5 will use this for the real p99 < 500 ms target"). Row stays PARTIAL for exactly that residual: the INV-S2 bound itself is unproven until measured over real propagation (Session 5 owns it; the legend's "sim with real propagation" level). **Probative-value warning, kept on the record:** the legacy `#edgeStalenessStaysWithinBoundsUnderNormalConditions` still self-drives a 20ms sync loop stamping commitTs = sync time — it remains a self-measuring smoke test (CM-049) and is NOT evidence for this row; the distribution sim test is. |
| CT-03 | §2 behavior on violation, STALE row (contract:70): "Set `X-Configd-Stale: true` header on all read responses" | PROCESS | `EdgeNodeIntegrationTest#staleHeaderAndReadinessDegradeAfterSourceLoss` (PROCESS), `EdgeHttpServerTest#staleHeaderSetOnAllReadsWhenStalePlus` | C2 | PASSING | **FLIPPED at the C2 audit (2026-06-11, bodies read):** the serving surface exists (`EdgeHttpServer`; this clause's RR-001 indictment answered). Process level: a real edge process loses its source (server killed mid-run), the ADR-0039 frontier freezes past 500ms, and `X-Configd-Stale: true` appears on reads over the real wire — the data is still served (serve-stale-with-notification; only cursor-behind refuses). HTTP level: the header is set on ALL reads while STALE+, hits AND misses (the clause's "all read responses"). The §11 fan-out >80% client signal (architecture:419) now has its header. |
| CT-04 | §2 behavior on violation, STALE row (contract:70): "Increment `configd.edge.staleness_violation_total` counter" | UNIT + GATE | `EdgeNodeMetricsTest` (`#staleTransitionIsCountedOncePerEntry`, `#stalePassThroughToDegradedCountsOnce`, `#bootStateDoesNotCountAsTransition`, `#everySeriesIsRegisteredEagerly`), `EdgeNodeIntegrationTest` (counter moved at a live `/metrics` after real source loss) | C2 | PASSING | **FLIPPED at the C2 audit (2026-06-11, bodies read):** `configd.edge.staleness_violation` exists, eagerly registered (RR-013), and counts STALE+ *entries* exactly once per transition (staying STALE does not re-count; each re-entry counts; a single observed CURRENT→DEGRADED jump counts once; the boot state is not a transition). Exported as `configd_edge_staleness_violation_total` and observed moving at process level even with NO live stream (the disconnected-pump path — the state where the counter matters most). Distinct from `invariant.violation.staleness_bound` as the clause requires. GATE half (consolidated checklist presence test) remains delegated to CT-38, which stays open. |
| CT-05 | §2 behavior on violation, DEGRADED row (contract:71): "emit alert. Edge node reports unhealthy to load balancer. Continue serving stale data." | PROCESS | `EdgeNodeIntegrationTest#staleHeaderAndReadinessDegradeAfterSourceLoss` (PROCESS), `EdgeHttpServerTest#livenessAlways200AndReadinessGatesOnDegraded` | C2 | PASSING | **FLIPPED at the C2 audit (2026-06-11, bodies read):** C2 defined the health surface the old row called missing — `/health/ready` 503 at DEGRADED+ (and at boot: a never-synced edge is not ready), `/health/live` unconditionally 200 (process-liveness, not staleness). Process level: real source loss → readiness flips 503 past the 5s threshold while liveness stays 200 and stale data keeps being served (200 + stale header). "Continue serving stale" asserted while STALE; holds at DEGRADED by construction — the serving path is staleness-state-independent except the CT-37 strong-read fail-close. "Emit alert" = the `edge_staleness_state` gauge + the CT-04 counter (series exist + move; alert *wiring* is Session 6's, charter rule 8 satisfied). |
| CT-06 | §2 behavior on violation, DISCONNECTED row (contract:72): "trigger re-bootstrap sequence. Attempt snapshot catch-up from regional relay." | PROCESS/SIM | `EdgeReBootstrapOnDisconnectTest` | C3 | UNIMPLEMENTED | Charter §4 C3 owns "snapshot+delta re-bootstrap" recovery; the DISCONNECTED *trigger* sits in C2's staleness state machine (split noted). The replay seam exists and is boundary-proven (CT-20); the trigger→re-bootstrap wiring at the edge does not. **C1 progress (audit 2026-06-11):** the re-bootstrap *mechanism* (demotion→chunked snapshot→contiguous resume) now exists and is wire-proven server-side (`FanOutServerIntegrationTest`); the DISCONNECTED *trigger* is now ADR-0039-governed (frontier measure). "Regional relay" is superseded topology language (architecture §5 banner, ADR-0030) — the real source is the fan-out service's `ReplaySource`; flag for the consolidated contract pass. **C2 progress (audit 2026-06-11):** the DISCONNECTED *trigger* now exists and fires exactly once per entry — `EdgeNodeMetricsTest#disconnectedTransitionFiresTheRebootstrapSeamOnce` (the `EdgeNodeMain.rebootstrapHook()` seam + `edge_rebootstrap_triggered_total`; boot state excluded — process start is bootstrap C5, not re-bootstrap), detected even with no live stream. The seam is a STUB: nothing re-bootstraps yet — row stays UNIMPLEMENTED with C3 (ADR-0040 orchestration). |
| CT-07 | §2 state machine (contract:69-72): CURRENT→STALE→DEGRADED→DISCONNECTED at 500ms/5s/30s, reset on update | UNIT | `StalenessTrackerTest` (reworked: `$AfterRecordUpdate`, `$ResetFromAnyState`, `$BoundaryValues`, `$HeartbeatFrontier`), `ConsistencyPropertyTests$StalenessUpperBoundTest` (`#stalenessStateTransitionsAreCorrect`, `#idleButHeartbeatingEdgeStaysCurrentAndBehindHeartbeatDoesNot`), `EdgeStalenessFrontierSimTest#partitionedEdgeWalksStaleDegradedDisconnected` (SIM corroboration) | C2 | PASSING | **FLIPPED at the C2 audit (2026-06-11, bodies read):** the CT-01 prerequisite landed, so the threshold transitions are now contract-true — driven by a TRUE STALL (BOTH deltas and heartbeats withheld; the frontier freezes while wall-now marches past it), reset on a fresh commit-ts update from any state. The exact defect ADR-0035 rejected relabeling for is now pinned the right way around: an idle-but-heartbeating edge (`latestSeq == cursor`) stays CURRENT across 250s, and a heartbeat carrying `latestSeq > cursor` does NOT reset state (real data-age lag surfaces — `#behindHeartbeatDoesNotAdvanceFrontierAndShowsLag`). Threshold table unchanged per ADR-0039, exact `> threshold` boundaries pinned (500/5000/30000 exactly-at cases). Initial state DISCONNECTED (no frontier known). |
| CT-08 | ADR-0035 handoff item 4: "≤ 50 ms NTP-skew assumption documented + a tripwire … flags negative or implausibly large staleness as a distinct metric" | UNIT | `StalenessSkewTripwireTest` (7), `EdgeClientCoreTest$SnapshotFrontierIntegrity`, `EdgeNodeIntegrationTest` (zero implausible samples on an ordered same-clock stream) | C2 | PASSING | **FLIPPED at the C2 audit (2026-06-11, bodies read):** the ADR-0039 §5 tripwire exists — a frontier > 50ms in the future (negative staleness beyond the documented, code-constant skew allowance) or a backwards frontier jump is counted on the dedicated `edge_staleness_implausible_total` and clamped/held, never silently trusted; within-allowance skew clamps WITHOUT counting (no false positives); both the commitTs and heartbeat paths covered; clamping holds even with no counter wired; a normal lagging frontier never counts. Signal integrity defended: the part-(a) snapshot-cutover false-positive defect (ADR-0028 bodies stamp ts 0 → every legitimate cutover would have fired the tripwire, masking real skew) was caught and fixed — `#snapshotCutoverDoesNotTripImplausibilityCounter` — so the metric stays alertable (Session 6). |
| CT-09 | §3 INV-M1 (contract:92): "Once a client reads at version V from any edge node, all subsequent reads by the same client return values at version >= V" | PROCESS | `ConsistencyPropertyTests$MonotonicReadTest` (3 tests, exists), `LocalConfigStoreTest$MonotonicReads` (4 tests, exists); owed: same property through the C2 serving surface | C2 | PARTIAL(unit) | Library-level coverage is real (cursor at/behind serves; ahead refuses). The clause says "any edge node" for "the same client" — needs the C2 process surface + multi-edge sim once the wire exists. |
| CT-10 | §3 mechanism (contract:99) + §8 `monotonic_read` row (contract:232): cursor-behind read "returns NOT_FOUND immediately (and increments `invariant.violation.monotonic_read`) … does not serve stale data" | UNIT (assertion site = proper level per §8) | `LocalConfigStoreTest$MonotonicReadInvariant#cursorAheadOfStoreFiresMonitor`, `#inTestModeCursorAheadThrows`, `#cursorAtOrBehindDoesNotFireMonitor`, `$MonotonicReads#readWithCursorAheadOfStoreReturnsNotFound` | C2 | PASSING | Verified by reading: asserts NOT_FOUND **and** `invariant.violation.monotonic_read == 1` **and** test-mode AssertionError. Satisfies §4.5 "assertion observed firing". Re-runs through C2 surface ride CT-09. |
| CT-11 | §3 INV-M2 / Edge Failover steps 1-2 (contract:102-104, INV-M2 :113-115): client reconnects to a different edge with cursor; new edge checks `local_version >= cursor` | PROCESS | `EdgeFailoverTest` (PROCESS: subscribed endpoint killed mid-stream → reconnect to a DIFFERENT fan-out endpoint carrying the resume cursor → cursor-monotonic resume; `edge_reconnects_total` moved), `ConsistencyPropertyTests$MonotonicReadFailoverTest` (4, in-process corroboration) | C2 | PASSING | **FLIPPED at the C2 audit (2026-06-11, body read):** the exact owed scenario now runs at process level — two fan-out endpoints over the SAME ADR-0034 boundary (what a second fan-out node drains in production), the subscribed one killed mid-stream, the edge reconnects to the other carrying `resumeCursor = core.cursor()` (the §3 handshake cursor; `failoverResumeCursor` reserved-field contract), and EVERY response on the way is either the consistent refusal or version ≥ cursor (`pollUntilServed` enforces it per response — cursor-monotonic across the reconnect, charter §4 C2 "test it explicitly"). Endpoint B reserved-but-not-started makes the refusal window deterministic, not a race. The `local_version >= cursor` check is the same INV-M1 store seam (CT-10). |
| CT-12 | §3 Edge Failover steps 3-4 **as amended** (contract:105-106): cursor-behind is "**refused immediately** — `404` + `X-Configd-Refused: cursor-behind`" + the edge's cursor; "the edge **never serves stale on a cursor-behind read**", uniform across steady state / catch-up / failover | PROCESS | `EdgeFailoverTest` (deterministic dead-endpoint refusal window + cursor-monotonic resume), `EdgeHttpServerTest#cursorBehindIsRefusedNeverServedStale`, `EdgeNodeIntegrationTest` (every catch-up response must be the refusal) | C2 | PASSING | **FLIPPED at the C2 audit (2026-06-11) — contradiction RESOLVED, passing against the amended clause.** §3 steps 3–4 were rewritten at the C2 contract pass per the **c2-c5-design-screen C2-4 ruling** (recorded in the contract pass, deliberately NOT a new ADR — a clarification of the ADR-0035/ADR-0039 consistent-refusal semantics; the ruling itself directs this row to "PASSING-against-the-amended-clause"). The refusal is strictly STRONGER than the old block-then-serve-stale text (not a silent weakening); the dangling "same timeout as above" is gone. Process-proven in a deterministic window (NO endpoint reachable, so refusal-during-catch-up cannot race): repeated refusals at the post-kill cursor (404 + header + the edge's real cursor + `edge_read_refusals_cursor_behind_total` + `invariant.violation.monotonic_read`), while the cursorless read legally serves the old value. HTTP matrix pins refusal vs true-miss vs malformed-cursor (404+header / plain 404 / 400). |
| CT-13 | Derived §3+§4: per-session monotonic reads **across edge restart** — edge crashes, loses cache, client reconnects with cursor → edge must refuse (NOT_FOUND / fail over), never serve pre-crash old data | PROCESS/SIM | `MonotonicReadAcrossEdgeRestartTest` (2 — SIM, via `EdgeActor` over the production `EdgeClientCore`); owed (C3): the restart re-bootstrap orchestration + a process-level restart leg | C2/C3 | PARTIAL(unit) | **C2 half LANDED at the C2 audit (2026-06-11, body read):** the silently-droppable clause is now pinned through the real INV-M1 seam — crash loses the cache; restart at cursor 0; a held cursor-5 read REFUSES (test-mode AssertionError via the monitor-wired store); a partial re-bootstrap snapshot at 3 (< cursor) STILL refuses (the cursor check runs against the **post-bootstrap** version, exactly the dropped clause); only a snapshot ≥ the cursor serves again, and it serves post-bootstrap state, never pre-crash data; a cursorless read never resurrects the pre-crash value. Still owed (C3, the row's other half): the same property across a REAL edge-process kill/restart (`EdgeNodeMain` restarted with a held client cursor — the re-bootstrap path itself is C3's; the `rebootstrapHook()` seam is a stub today). |
| CT-14 | §4 INV-V1 (contract:147-148): "if e1 committed before e2, then S(e1) < S(e2)" (mutating entries) | SIM | `ConsistencyPropertyTests$SequenceMonotonicityTest` (3 tests incl. follower replicas + leader failure) | — (S2-owned input to the edge cursor) | PASSING | Verified by reading: listener on the real `ConfigStateMachine` over real `RaftNode`s in deterministic sim asserts strictly increasing S, incl. across failover. This is the §7-designated level. |
| CT-15 | §4 INV-V2 (contract:150-153): consecutive mutating commits have `S(e_{i+1}) = S(e_i) + 1` (gap-free over the mutation stream) | SIM | `ConsistencyPropertyTests$SequenceGapFreeTest` (3 tests incl. `gapFreeAcrossLeaderFailover`) | — (S2-owned input) | PASSING | Verified by reading: asserts `prev + 1` equality per consecutive pair, incl. across failover. Structural note (contract:251): no standalone runtime assertion — enforced by single-writer apply; this sim test is the designated check. |
| CT-16 | §4 gap detection (contract:138) / architecture §7 (:275-283): "`received_seq == last_applied_seq + 1` → apply; `>` → GAP, enter catch-up; `<=` → duplicate/stale, discard" at the edge | PROCESS | `DeltaApplierTest$GapDetection` (4 tests), `$StaleDeltaDetection` (2 tests), `LocalConfigStoreTest$BasicOperations#deltaVersionMismatchThrows` (exist); owed: the same rule on the **CommitNotification seq cursor** through the edge drain loop with GAP→replay | C3 | PARTIAL(unit) | Charter §4 C3 explicitly owns "Cursor-gap detection on the edge" (:127) — remapped from C2. Existing coverage is delta-`fromVersion`-based on `DeltaApplier`/`LocalConfigStore`. **C1 progress (audit 2026-06-11):** the notification-seq drain now exists and runs in SIM — `EdgeActor` applies CommitNotifications through the real `DeltaApplier` gap/stale logic (cursor = applied seq), fed by the production `FanOutSessionCore` via `C1StreamDriver`; GAP→ack-stall→demotion→snapshot-heal exercised by the 507-seed sweep. Interaction with batching is CT-17 (ADR-0038 **Accepted**: the chain is verbatim, so the seq+1 rule stays exact). **C2 progress (audit 2026-06-11):** the edge-**process** drain now exists and routes every inbound frame through the production core's seq rule (`EdgeStreamClient` → `EdgeClientCore`; gap/stale/contiguous pinned at core level incl. mid-batch GAP acking the REAL cursor — `EdgeClientCoreTest$NotifyHandling` — and GAP→forward-snapshot heal, `$SnapshotHandling#forwardSnapshotAfterGapHeals`; `EdgeNodeIntegrationTest` applies through this drain at process level). Still owed (C3): the explicit edge-side GAP→replay orchestration (resubscribe-with-cursor per the C3-1 screen ruling / ADR-0040) exercised at process level. |
| CT-17 | Charter §4 C1 (:108-109): "Coalescing: multiple updates to the same key in flight may collapse to the latest, but version cursors must never skip in a way that breaks gap detection (define and test the exact rule)" — vs §4's seq+1 check | BOUNDARY + PROCESS | `FrameBatchingChainIntegrityTest` (exists, GREEN — boundary property, 400 tries incl. overflow→GAP→snapshot paths); process corroboration: `FanOutServerIntegrationTest` verbatim in-order NOTIFY + post-snapshot contiguous resume over a real socket | C1 | ADR-RENEGOTIATED(adr-0038) | **ADR-0038 Accepted (ratified 2026-06-11, c1-design-review §A2).** The charter's "may collapse" is renegotiated to **may not**: no server-side coalescing of signed payloads (the relay holds no signing key); throughput = **frame-level batching**, chain + signatures intact; the exact rule = "the stream is the contiguous applied-mutation seq chain; nothing collapsed or skipped; any observed skip is a real gap → catch-up". Closed by ADR reference **with executable evidence**: verified by reading the test body (audit 2026-06-11) — NOTIFY-batch concatenation is a verbatim (no merge/fabrication), strictly-ascending, contiguous-over-publish-order subsequence; only allowed discontinuity is an explicit SNAPSHOT_BEGIN..END boundary, after which resume > snapshot seq. `WatchCoalescer` stays off the fan-out drain (re-verified: zero fanout/wire consumers). |
| CT-18 | ADR-0034 §2/§5 (+ADR-0036): "`readSince` NEVER returns a partial/duplicated run that silently skips evicted notifications"; non-GAP runs are strictly-ascending, contiguous, no-dup, no-skip under concurrency | BOUNDARY + GATE (jcstress) | `FanOutBufferRaceTest#concurrentReaderNeverSeesDuplicateOrSkippedSeqAndEventuallySeesAll`, `#readerPacedSeesContiguousStreamExactlyOnce`; jcstress `io.configd.jcstress.FanOutBufferReadSinceTest` (`ExactlyFullWrap` et al.) | C1 | PASSING | Verified by reading (race test asserts no `seq <= prev` in any OK run, 4 readers × 200k writes; jcstress outcome 9 = FORBIDDEN torn class). RR-096/ADR-0036 fixed the eviction order; post-fix jcstress `quick` clean. **Gate-3 must run the FanOutBuffer eviction jcstress variants in `quick` mode** — gate-2's `sanity` demonstrably missed RR-096 (ADR-0036 §Verification). |
| CT-19 | ADR-0034 §3 overflow policy: bounded ring 10,000, drop-oldest, `fanout_buffer_dropped_total` increments, lagging cursor gets **GAP** (never truncated run); caught-up cursor gets `Ok([])` | BOUNDARY | `CommitNotificationSourceTest#boundSustainedAppendsNeverGrowBeyondRing`, `#overflowIncrementsDropCountAndStaleCursorGetsGap`, `#caughtUpCursorReturnsEmptyOkNotGap` | C1 | PASSING | Verified by reading: drop count == evictions; GAP carries `oldestRetainedSeq`; size never exceeds cap over cap×100 appends. Metric wired in `ConfigdServer`/`PrometheusExporter` (ADR-0034 §3). |
| CT-20 | ADR-0034 §4 + handoff step 2: GAP → `replayFromSnapshot()` → apply snapshot wholesale, cursor=S, resume tail = "**exactly-once over effect** … no hole and no double application" | BOUNDARY | `CommitNotificationSourceTest#replayThenTailObservesEveryMutationEffectExactly` (25 seeds, randomized overflow/replay/tail interleaving, byte-equality vs authoritative model) | C1 | PASSING | Verified by reading. This proves the **boundary**; the same property through wire + edge store is CT-24 (not covered by this row). |
| CT-21 | ADR-0034 §1/§6 + ADR-0035: notification carries `commitTimestampMillis` = leader wall clock at apply (the §2 staleness clock), emitted on the stream | BOUNDARY | `CommitNotificationSourceTest#commitTimestampIsCarriedThrough` | C1 | PASSING | Verified by reading (asserts the timestamp survives the boundary; assertion message cites ADR-0035). Producer half of CT-01 — the consumer half is what S3 owes. |
| CT-22 | ADR-0034 handoff step 3: "**Never** consume `FanOutBuffer.deltasSince` (non-atomic, RR-066) — only `readSince`" | GATE (static guard) | `NoDeltasSinceOnConsumerPathTest` (GREEN, 2; pattern: `NoBlockingConnectOnConsensusPathTest`) | C1 | PARTIAL(unit) | **C1's REQUIRED gap FIXED** (04384b5, re-verified at the C2 audit 2026-06-11): `SCAN_ROOTS` now covers `configd-distribution-service/src/main`, `configd-server/src/main` (the production drain), `configd-edge-cache/src/main`, `configd-testkit/src/test`; self-checks the roots resolve (no vacuous pass). **NEW gap, same class (C2 audit):** `configd-edge-node/src/main` — created in 37cf3c6, AFTER the hardening commit — is **not** in `SCAN_ROOTS`. Grep today: zero `deltasSince`/`FanOutBuffer` refs in that module (the guarantee holds), but the guard cannot catch an edge-node regression — exactly the recurring failure mode this row exists to stop. One-line addition owed (named for the lead; code change, out of doc-only lane). Flips to PASSING when the roots cover every consumer module incl. configd-edge-node. |
| CT-23 | ADR-0034 §1 + handoff step 1: edge verifies the F-0052 `signature`/`epoch`/`nonce` on each delta and rejects replays **before applying** | PROCESS | `EdgeNodeIntegrationTest` (PROCESS: `#writePropagatesOverSignedChainAndServesWithCursor` — real Ed25519 chain via the real `VerifyKeyExporter`, verified-applies, 0 rejections; `#edgeWithoutVerifyKeyRejectsTheSignedChainFailClosed` — rejected fail-closed, NEVER applied), `EdgeClientCoreTest$VerificationSeam` (tampered signature rejected on the receive path; rejection ≠ gap), `DeltaApplierTest$SignatureVerification` (7) + `$EpochPersistence` (4: replay rejected **across restart**, corrupt sidecar demoted) | C2 | PASSING | **FLIPPED at the C2 audit (2026-06-11, bodies read):** verification now sits ON the C2 receive path — `EdgeClientCore.onNotify` routes every wire-delivered delta through the verifier-wired `DeltaApplier` BEFORE apply — and that wiring is proven live at PROCESS level in both directions (a real server-signed chain verifies and applies; an unverifiable chain is rejected fail-closed: 0 applied, store never advances). Rejections counted on their own `edge_verify_rejections_total`, never folded into the gap signal (the honest-gap-series split). Honest residuals (noted, not blocking): no literal mid-stream tampered-FRAME injection over a socket — frame integrity is the codec CRC32C (property-tested, CT-41) and the tampered-SIGNATURE case is pinned at the same core receive seam the process test proves live; epoch-replay-across-restart remains applier-level (`$EpochPersistence`). |
| CT-24 | Charter §4 C5 (:139-141): "Zero-state edge joins under sustained concurrent writes: snapshot transfer + cutover to live stream with no gap and no duplicate-application divergence (idempotent apply or exact cutover cursor — choose, justify, test)" + ADR-0034 handoff steps 1-2 + architecture §10 bootstrap diagram | PROCESS/SIM | `EdgeBootstrapUnderSustainedWritesTest` | C5 | UNIMPLEMENTED | The headline RR-001 acceptance: writes flow *during* snapshot transfer + cutover to tailing; final edge view byte-equals control-plane state; no mutation effect applied twice. Judged by the V1 **snapshot–delta equivalence** invariant (charter:85-86). Boundary analogue exists (CT-20) — this is the end-to-end form. Reuse `AdversarialSim`/`HistoryRecorder` (handoff §4) rather than building a new harness. |
| CT-25 | Architecture §7 Subscription Model (:293-297) / charter §4 C1 (:106-107): "per-key / prefix / full-store — implement what §7 specifies; descope by ADR if needed" | PROCESS | C1 half: `FullChainDeliveryTest`, `FanOutServerIntegrationTest` (PROCESS); C2 half: `EdgePrefixStorageFilterTest` (12), `EdgeClientCoreTest$StorageFilterThroughCore` (3); matching engine: `PrefixSubscriptionTest` | C1/C2 | ADR-RENEGOTIATED(adr-0038) | **CLOSED at the C2 audit (2026-06-11) — exactly per the recorded plan** (`c1-contract-qa-audit.md`: "row closes when `EdgePrefixStorageFilterTest` lands"): both halves of the ADR-0038-renegotiated clause are now executable. C1 half (process-proven at the C1 audit): full signed chain delivered to every subscriber incl. non-matching keys, in order; prefix and full-store subscribers drain byte-identical chains. C2 half (bodies read): the **edge-side storage filter** — matching keys stored; non-matching mutations advance the version chain WITHOUT storing (from/to preserved, all-filtered batches still advance, gap detection unaffected — `$ChainValidationUnaffected`); reads outside the subscription NOT_FOUND; `secure/` ALWAYS stored regardless of subscription (CT-37 store-and-fail-closed-serve); empty subscription = full store; `filterForStorage` deterministic/idempotent (the read-store lockstep contract, also proven through the production core). "Per-key" N/A-by-construction at transport; "full-store" universal. Residual (not load-bearing for the renegotiated clause): no process run with a non-empty prefix subscription yet — the serving surface consumes the same lockstep-filtered store; C6 Compose should include one prefix-subscribed edge. |
| CT-26 | Charter §4 C1 (:110-112): "bounded per-subscriber queues, explicit overflow→catch-up demotion (a slow subscriber is switched from streaming to replay mode, never an unbounded queue, never a silent drop without cursor evidence). Every policy threshold is a named config with a metric." + architecture §7 (:260-267) / §11 (:419-420) | PROCESS | `SubscriberQueueBoundTest` (property: unacked frames ≤ queueFrames under any interleaving; 80% warn metric fires), `SubscriberOverflowDemotionTest` (overflow→CATCHUP with `DemotionEvent` cursor evidence + metric + on-wire `DEMOTED_TO_CATCHUP` notice, non-fatal; transport would-block demotes; no committed effect lost across demotion), `FanOutServerIntegrationTest` demotion leg (PROCESS: real-socket unacked flood → demotion → chunked snapshot → contiguous resumed tail; demotion counter moved at `/metrics`) | C1 | PASSING | Verified by reading all bodies + fresh surefire (audit 2026-06-11, 0 failures). Every threshold is a named `FanOutConfig` field with a documented metric (queueFrames/queueWarnPct/batchMaxNotifications/batchMaxBytes/ackLagDemoteSeqs/heartbeatMs/idlePollMs/snapshotChunkBytes), eagerly registered (`RegistryFanOutSessionMetrics`, RR-013; exact exported names pinned by `RegistryFanOutSessionMetricsTest`). Ack-lag transition unit-pinned at an explicit threshold (`FanOutSessionCoreTest#ackLagBreachDemotes`); the sim-tuned `ackLagDemoteSeqs=2` (prod 8192) is a workload-scale knob, **not** evidence-load-bearing — the demotion tests above run prod ack-lag 8192 and the live server uses `defaults()`. Arch §7 credit numbers superseded on the record: frames/bytes accounting over the ADR-0034 ring (ADR-0038 + C1 as-built note; review B-3). |
| CT-27 | Architecture §7 Slow Consumer Policy (:288): "0 credits for > 10s → Warning log + metric"; charter §4 C4 (:135): "Each transition: a test, a metric, a structured log event" | PROCESS + GATE | `SlowConsumerWarningTransitionTest` | C4 | UNIMPLEMENTED | `SlowConsumerPolicy` class exists in `src/main` but has **zero dedicated tests** (only `assertNotNull` wiring in `ConfigdServerTest:254`) and is consumer-unreachable (RR-034-class orphan). ADR-0034 handoff item 5 additionally requires wiring it to a **GAP-rate** signal. |
| CT-28 | Architecture §7 (:289): "0 credits for > 30s → Disconnect from tree, mark as quarantined" — with test+metric+log (charter §4 C4) | PROCESS + GATE | `SlowConsumerQuarantineTransitionTest` | C4 | UNIMPLEMENTED | Same orphan status as CT-27. Each transition is a separate row per charter §4 C4 (test+metric+log each). |
| CT-29 | Architecture §7 (:290): "Quarantined → Must re-bootstrap via catch-up protocol" — with test+metric+log (charter §4 C4) | PROCESS + GATE | `QuarantineReBootstrapTest` | C4 | UNIMPLEMENTED | Re-bootstrap path = CT-06/CT-24 machinery; this row pins that a quarantined consumer is *forced* through it (cannot resume tailing with its old cursor). |
| CT-30 | Architecture §7 (:291): "3 quarantines in 1 hour → Marked as unhealthy, removed from distribution tree" — with test+metric+log (charter §4 C4) | PROCESS + GATE | `RepeatQuarantineUnhealthyTest` | C4 | UNIMPLEMENTED | Needs a clock-driven test (SimulatedClock) — no sleeps (evidence discipline, handoff §6). Charter §4 C4 (:135-136) additionally requires the **full state-machine walk in the simulator** (a deliberately-lagging edge actor) — that walk is also a named gate-3 step (charter:160). |
| CT-31 | Architecture §7 Catch-up Protocol (:269-273): gap < window → stream deltas; gap > window → "chunked snapshot, then streams deltas from snapshot point. Chunked snapshot: 1 MB chunks, CRC per chunk, resume on failure." | PROCESS | Built half (C1): `FanOutSessionCoreTest#gapMidStreamDemotesThenSnapshotsThenResumesTail`, `SubscriberOverflowDemotionTest#noCommittedEffectLostAcrossTheDemotionBoundary`, `FanOutServerIntegrationTest` snapshot leg (BEGIN/chunkCount/END agree, resume > snapshot seq, over a real socket), `EdgeSnapshotCodecTest`; owed (C3): `CatchUpProtocolTest`, `SnapshotChunkResumeTest`, `ReplayHorizonBoundaryTest` (charter C3's "horizon-boundary case, under concurrent writes") | C3 | PARTIAL(unit) | **C1 built the chunked-snapshot half over the boundary** (audit 2026-06-11): 1 MiB chunks (`snapshotChunkBytes`, codec-capped, at-cap golden fixture) + CRC32C per chunk frame (codec trailer; single-bit corruption rejected, `EdgeFrameCodecPropertyTest`), demotion→snapshot→contiguous-resume proven at session level and on the wire. Small-gap catch-up = `readSince` Ok run (CT-18/19/20, PASSING). Still owed (C3): edge-side gap→protocol selection, the horizon-boundary case under concurrent writes, and chunk-level **resume on failure** — C1's behavior on a lost snapshot is re-demote→re-send whole (self-healing, design-note bug fix 1): a deviation from §7's wording the C3 design note must implement or renegotiate (`c1-contract-qa-audit.md` REQUIRED gap 2). `CatchUpService` is still a zero-test orphan; the §7 WAL-delta wording reconciliation also remains with C3. |
| CT-32 | Architecture §8 Negative Caching (:326-329): key-only index + "Bloom filter for fast rejection of non-existent keys (~10 bits/key, 1% false positive rate)" | PROCESS | `BloomFilterTest` (no false negatives; FPP < threshold), `BloomFilterPropertyTest` (exist); owed: wiring into the edge read path | C3 | PARTIAL(unit) | `BloomFilter` is an **orphan** — zero `src/main` consumers (verified by grep; RR-034-class). Charter §4 C3 (:130-131): "implement or explicitly descope by ADR (do not leave it ambiguous)". If descoped, flip to ADR-RENEGOTIATED(adr-ref). ADR-0038 §2 (now Accepted) notes negative caching still applies for reads outside the subscription filter. |
| CT-33 | Architecture §8 Poison-Pill Handling (:331-334): dual-TTL, version format checks, "Circuit breaker: if value fails validation, serve previous known-good version. Emit `configd.edge.poison_pill` metric." | PROCESS | `PoisonPillDetectorTest` (quarantine after max retries, success resets, release, listener — exists); owed: wiring + serve-previous-known-good + metric emission | C3 | PARTIAL(unit) | `PoisonPillDetector` is an **orphan** — zero `src/main` consumers; the serve-known-good circuit breaker and the named metric do not exist anywhere. Charter §4 C3 (:130-131): **implement or explicitly descope by ADR.** |
| CT-34 | Hot-path law — charter §6 rule 3 (:170-171 "Inherited hot-path law applies to the edge read path … Violations are P1 findings even in your own new code"), §4 C2 (:120-121 "verify with a brief JMH gc-profile run"), §5 gate-3 (:161 "Brief JMH gc-profile check: zero steady-state allocation on the edge read path"); origin PROMPT §3.5 + hard rule 6; architecture §3:116-122 | GATE (JMH gc-profile) + UNIT | `LocalConfigStoreReadBenchmark` (landed, run on this box), `LocalConfigStoreTest$ConcurrentAccess#multipleReadersOneWriter`; owed: the gate-3 mechanical `-prof gc` step (`gates/gate-3.sh` does not exist yet) | G3 | PARTIAL(unit) | **C2 audit 2026-06-11:** the owed benchmark now exists (`io.configd.bench.LocalConfigStoreReadBenchmark`: getMiss/getIntoHit/getHit/getHitWithCursor across sizes 1k/10k/100k) and was run with `-prof gc` on this box (figures recorded in its Javadoc + design note §6): `getMiss` ≈6ns/**0 B/op**, `getIntoHit` ≈117ns/**0 B/op** (the VDR-0001 strict-zero-alloc API), `getHit` ≈89ns/**32 B** = exactly the one documented `ReadResult` (matches `VersionedConfigStore`'s established figure); the INV-M1 cursor gate adds a branch, no alloc. **Scope ruling, kept visible for sign-off:** the law is declared to bind the in-process read path (`LocalConfigStore.get`/`EdgeClientCore.get`); the HTTP shell above it allocates per request and is priced as NOT the §3 law's path (benchmark Javadoc + design §3.8/§6) — review-architect ratifies or contests at the C2 sign-off. Row stays PARTIAL for the GATE leg: no `gates/gate-3.sh` yet, and no machine-readable saved run artifact under `perf/results/` — the gate must re-run and mechanically assert the 0 B/op rows. |
| CT-35 | §6 INV-RYW1 (contract:182-187, :199-203): after ack `seq=S`, same-region reads with `cursor.version=S` see version ≥ S; cursor-behind edge returns NOT_FOUND immediately (no blocking ryw_timeout) | PROCESS | `EdgeNodeIntegrationTest#writePropagatesOverSignedChainAndServesWithCursor` (PROCESS), `ConsistencyPropertyTests$ReadYourWritesTest` (4, in-process cursor semantics) | C2 | PASSING | **FLIPPED at the C2 audit (2026-06-11, body read):** the ack-S→cursor-S loop is now proven through the REAL propagation path — HTTP write → `Committed: seq=S` (the ADR-0033 ack) → commit-notification boundary → fan-out wire → verified apply → edge HTTP read with `X-Configd-Cursor: S`. The poll discipline IS the clause: every pre-propagation response must be the immediate refusal (404 + cursor-behind — no blocking ryw_timeout, per §6:187 as amended), and the served response is asserted version ≥ S — never below the cursor on ANY response. Repeated for a second write (the carried-forward cursor stays monotonically satisfiable). Single-edge, single-region — exactly the clause's "same region" scope; multi-edge "any edge node" rides CT-09. |
| CT-36 | §6 Cross-Region (contract:189-195): "NOT GUARANTEED (without opt-in)"; opt-in via cursor or control-plane ReadIndex | — | — | — | N-A(ADR-0030 single-region topology — no cross-region deployment exists; the ReadIndex opt-in path is S2-owned control plane, tested there) | Disclaimer clause; nothing edge-side to test until a multi-region topology is re-specified (contract §5.3). |
| CT-37 | §9 / §1:37 / RR-020 (+handoff §3): strong-read keys (default `secure/`) are "**never** served" from local/bounded-stale state — "The edge MUST NOT serve strong-read keys from stale local state — route them to the control-plane ReadIndex or fail closed" | PROCESS | `EdgeStrongReadFailClosedTest` (PROCESS), `EdgeHttpServerTest#strongReadKeyFailsClosedEvenThoughStored`, `StrongReadKeyClassTest` (10, the shared predicate), `EdgePrefixStorageFilterTest$StrongReadKeysAlwaysStored` | C2 | PASSING | **FLIPPED at the C2 audit (2026-06-11, bodies read):** the storage/serving half is decided and proven — **store-and-never-serve** (C2-2 screen ruling): the signed chain delivers `secure/` to the edge (ADR-0038 suppression detectability; delivery half CT-25), the store keeps it (the V1 snapshot–delta equivalence invariant stays a plain full-store byte-compare; the stored bytes are byte-checked through the core), and the serving path fail-closes BEFORE the store is consulted — 503 + `X-Fail-Closed: strong-read` (RR-020-consistent), with or WITHOUT a satisfiable cursor, no value bytes in any response body, `edge_read_refusals_strong_read_total` moving. The process test also pins the RR-098 residual boundary: the secret's bytes never land under `--data-dir` (epoch sidecar metadata only — disk-swept against the literal secret). The enlarged exfiltration surface is priced, not hidden: **RR-098** (P2, owner S5; design note §8). Control-plane half unchanged (S2 `StrongReadPolicy`). |
| CT-38 | Charter §6 rule 8 (:181-183): "Emit production metrics (staleness, cursor lag, queue depths, policy transitions) as you build — Session 6 … cannot wire series that don't exist"; §7 DoD (:195): "Metrics emitted for every policy threshold and propagation stage (named list in handoff)". Required series checklist: edge staleness gauge + histogram; edge cursor lag (`latestSeq − last_applied_seq`); per-subscriber queue depth; slow-consumer state-transition counters (one per CT-27..CT-30 transition); fan-out connected-subscriber count; propagation probe histograms (charter §3 V2); `configd.edge.staleness_violation_total` (CT-04); `configd.edge.poison_pill` (CT-33) | GATE | `RegistryFanOutSessionMetricsTest` (exact exported names + callback wiring + other-bucket fallback), `FanOutServerIntegrationTest` `/metrics` moved-assertions (exist, GREEN); owed: `EdgeMetricsContractTest` (the consolidated presence+emission gate over the full checklist) | G3 | PARTIAL(unit) | **C1's `edge_fanout_*` series now emitted + test-pinned** (audit 2026-06-11, read from `RegistryFanOutSessionMetrics` — eager registration, RR-013): `edge_fanout_notify_batches_total`, `edge_fanout_notify_batch_size` (histogram), `edge_fanout_heartbeats_total`, `edge_fanout_slow_consumer_warnings_total`, `edge_fanout_snapshot_transfers_total`, `edge_fanout_demotions_{queue_overflow, ack_lag, gap, transport_block, other}_total`, `edge_fanout_sessions_closed_{server_shutdown, protocol_violation, frame_corrupt, bad_wire_version, auth_fail, gap_unrecoverable, transport_gone, other}_total`, `edge_fanout_queue_depth` (gauge), `edge_fanout_connected_subscribers` (gauge). **Deviations (priced, not hidden):** design's `{reason=...}` labels → per-reason name suffixes (`MetricsRegistry` is label-free; unknown reasons land in `other`); queue depth is a process-level high-water gauge, not per-subscriber (needs a label-capable backend). Pre-existing & verified: `fanout_buffer_dropped_total`, `invariant.violation.monotonic_read`, `invariant.violation.staleness_bound`. **C2's edge-node series now emitted + test-pinned (C2 audit 2026-06-11):** `edge_staleness_ms` / `edge_staleness_state` (live-tracker gauges, read at scrape time), `edge_cursor_lag`, `configd_edge_staleness_violation_total` (CT-04 — done), `edge_applied_total`, `edge_gaps_total`, `edge_snapshots_applied_total`, `edge_verify_rejections_total`, `edge_reads_total`, `edge_read_refusals_{cursor_behind,strong_read}_total`, `edge_reconnects_total`, `edge_rebootstrap_triggered_total`, `edge_staleness_implausible_total` — eagerly registered (`EdgeNodeMetricsTest#everySeriesIsRegisteredEagerly`, RR-013; unknown refusal reasons rejected loudly, not silently bucketed), presence+movement asserted at a live `/metrics` (`EdgeHttpServerTest#metricsEndpointExportsTheContractualSeries`, `EdgeNodeIntegrationTest`, `EdgeFailoverTest`). **Row stays open for:** edge staleness HISTOGRAM (gauges only today), CT-27..CT-30 transition counters, V2 probe histograms, `configd.edge.poison_pill`, and the consolidated `EdgeMetricsContractTest` gate. |
| CT-39 | PROMPT §2.5 / RR-001 umbrella: "**Edge propagation correctness** — every committed write eventually reaches every live edge, bounded by the staleness contract"; charter §3 V1 "Eventual delivery" invariant (:83-84) + §4 C6 (:143-149) Compose scenarios | PROCESS (gate-3 E2E step) | Now proven (server half): `FanOutServerIntegrationTest` (real HTTP write → `Committed: seq=S` → verbatim NOTIFY over a real socket, incl. demotion/snapshot/heartbeat legs), `EdgePropagationBacklogTest` (RE-ENABLED, GREEN — the RR-001 executable backlog: sim delivery within bound + every live edge converges byte-equal via the production `FanOutSessionCore`), `EdgeAdversarialGateSeedSweepTest` (507 seeds, 0 safety violations), `EdgeLeaderKillScenarioTest`; owed: `EdgePropagationEndToEndTest` + the charter §4 C6 scripted Compose scenario (3 CP + ≥3 edge **processes**: sustained writes; leader kill mid-stream; edge partition → demotion/catch-up/convergence; fresh-edge bootstrap mid-load) | C6 | PARTIAL(unit) | The session headline. **RR-001's "zero committed writes can reach any edge" is no longer factually true at the server side** (audit 2026-06-11) — but the row needs the C2 edge PROCESS + C6 Compose E2E before it can close; RR-001 stays OPEN accordingly (charter §6 rule 6). Honesty: the 507-seed sweep records 6 delivery-bound liveness violations / raw convergence 62.1% under never-healed schedules (96.1% given quiet) — RR-095-class CP-liveness characterization, not C1 logic (EDGE-GATE-SUMMARY). C6 also owns the RR-095 stall-seed re-run; Compose runs must use **production `FanOutConfig.defaults()`**, not the sim-tuned thresholds (`c1-contract-qa-audit.md` gap 3). **C2 progress (audit 2026-06-11):** the edge-PROCESS half now exists — `EdgeNodeIntegrationTest` proves write → commit → wire → verified apply → cursor-bound read against a real `ConfigdServer` + real `EdgeNodeMain` (1 CP × 1 edge, loopback); `EdgeFailoverTest` adds endpoint-kill failover. Row still owed the C6 Compose E2E before it can close. |
| CT-40 | Charter §4 C2 (:115-117, incl. the in-charter ADR-0037 deviation note): separately runnable edge process with "mTLS consistent with the control plane's" — same `TlsConfig.mtls`/`TlsManager`, `setNeedClientAuth(true)`, bounded connect/handshake | PROCESS | `EdgeTransportMtlsTest` (3, PROCESS — the edge-CLIENT half), `FanOutServerMtlsTest` (server half, C1) | C2 | PASSING | **FLIPPED at the C2 audit (2026-06-11, bodies read):** the separately runnable edge process exists (`configd-edge-node`, shaded `EdgeNodeMain`) and its mTLS is "consistent with the control plane's" BY CONSTRUCTION — the same `TlsConfig`/`TlsManager` classes. Three process legs against a live mTLS `FanOutServer`: **trusted client cert** → subscribes AND applies a published notification through the stream (functional, not just a handshake); **rogue CLIENT cert** → never sees `SUBSCRIBE_OK`/heartbeats while its reconnect counter proves it kept trying (the timing-robust unusable-connection discipline, find0051); **rogue SERVER cert** → the CLIENT trust path refuses (F-0051 HTTPS endpoint identification). Wire edgeId deliberately ≠ cert DN (cert-DN-authoritative, RR-094 pattern). Honest corners (design note §7.5/§8, named not hidden): edge-side absent-client-cert not exercised edge-side (the edge always loads a keystore; server-side no-cert rejection is C1-proven); the CLI `TlsConfig.mtls` path is test-injected via `EdgeNodeMain.start(cfg, tls)` (keytool cannot produce the empty store password — the documented `ConfigdServerTest`/`FanOutServerMtlsTest` precedent, pre-existing debt class); bounded connect/handshake are implemented constants (1s/2s, mirroring `TcpRaftTransport`) not behavior-asserted under a stalled peer. |
| CT-41 | ADR-0037 (Accepted; edge wire discipline): separate `EdgeFrameCodec` + `EDGE_WIRE_VERSION`, "length prefix bounds-checked before allocation, version byte, type byte, CRC32C trailer, explicit frame cap … its own golden-fixture test from day one" | UNIT + GATE | `EdgeFrameCodecGoldenFixtureTest` (25 fixtures, GREEN), `EdgeFrameCodecPropertyTest` (11, jqwik), `EdgeCodecBoundaryTest` | C1 (built with C1(a) per ADR-0037's shared stack; C2 consumes it) | PASSING | Verified by reading (audit 2026-06-11): fixtures cover **every** `FrameType` (9) and **every** `ErrorCode` (10) via a `values()`-driven coverage tripwire (a new type/code cannot land without a golden entry), the empty-NOTIFY edge case, the **ADR-0038 batched NOTIFY form** (`notify_batch_signed.bin`), and the at-cap 1 MiB snapshot chunk (byte-pinned via full-frame CRC32C + round-trip decode); byte-equality asserted both directions (encode==golden hex AND golden decodes to canonical frame; `peekLength`/re-encode re-checked per entry). **Rebaseline rule documented** in the test Javadoc and `EdgeFrameGoldenBytes` ("READ BEFORE editing"): revert, or bump `EDGE_WIRE_VERSION` + regenerate via `EdgeFrameGoldenBytesGenerator`. Property test: round-trip, per-byte truncation, single-bit corruption → CRC error, cap rejection **pre-allocation**. Day-one requirement honored: fixture landed with the codec in `ca22214`. GATE leg = the fixture suite itself (wire drift fails the build by design). |

---

## Summary by status

(Post-C2 audit, 2026-06-11 — `docs/session-3/reviews/c2-contract-qa-audit.md`; previous:
`c1-contract-qa-audit.md`.)

- PASSING: CT-01, CT-03, CT-04, CT-05, CT-07, CT-08, CT-10, CT-11, CT-12, CT-14, CT-15, CT-18, CT-19, CT-20, CT-21, CT-23, CT-26, CT-35, CT-37, CT-40, CT-41 (21)
- PARTIAL(unit): CT-02, CT-09, CT-13, CT-16, CT-22, CT-31, CT-32, CT-33, CT-34, CT-38, CT-39 (11)
- FAILING-CAPTURED: none yet (0)
- UNIMPLEMENTED: CT-06, CT-24, CT-27, CT-28, CT-29, CT-30 (6)
- ADR-RENEGOTIATED: CT-17, CT-25 (both adr-0038) (2)
- N-A: CT-36 (1)

No contract clause is blocked on an open decision anymore: **CT-12 is resolved** — §3 Edge Failover
steps 3–4 amended at the C2 contract pass (consistent refusal; c2-c5-design-screen **C2-4 ruling**,
recorded in the contract, not a new ADR) and process-tested. **ADR-0039 is implemented**, not just
accepted: CT-01/07/08 PASSING on the frontier measure (idle proxy deleted); CT-02 stays PARTIAL only
for the Session-5 real-latency p99 measurement (mechanism proven at SIM). **CT-25 is closed** by
ADR-0038 reference now that its C2 storage-filter half landed (`EdgePrefixStorageFilterTest`) — the
exact condition the C1 audit recorded for closure.
Still open for design notes: CT-32/CT-33 (negative-cache + poison-pill implement-or-descope, charter §4 C3;
ADR-0040 governs the poison-pill gate), CT-31 (chunk-level resume-on-failure: implement or renegotiate;
horizon-boundary + WAL-delta wording — C3).
Remaining owed work by owner: C3 — CT-06 (re-bootstrap orchestration behind the now-tested trigger seam),
CT-13's process-restart leg, CT-16's GAP→replay orchestration, CT-31/32/33; C4 — CT-27..CT-30; C5 — CT-24;
C6 — CT-39's Compose E2E (+ CT-09's multi-edge form); G3 — CT-34's gate-3 `-prof gc` step, CT-38's
consolidated `EdgeMetricsContractTest`; CT-22 — the C1 scan-roots gap was fixed (04384b5) but the NEW
`configd-edge-node/src/main` module is not yet in `SCAN_ROOTS` (one-line addition owed; see row).

CONTRACT-MAP-SUMMARY: total=41 passing=21 partial=11 failing-captured=0 unimplemented=6 adr=2 na=1
