# Session 5 — Decision Log (for retroactive veto)

> Per charter §2/§13: the team lead resolves **technical/measurement-methodology** questions
> autonomously (read code, run the harness, reason it out — often via a fresh `opus` sub-agent), and
> takes the **conservative default** for **scope/sequencing/branch** decisions (checkpoint at a clean
> committed seam, never talk into a riskier path). Every such decision is logged here so the user can
> veto retroactively. Type = **TECH** (self-resolved technical) or **SCOPE** (conservative-default).

| # | Decision | Type | Rationale (and what was rejected) |
|---|---|---|---|
| D-01 | **Make CI real via push `session-4-chaos` + `workflow_dispatch`**, not a PR/merge to `main`. | SCOPE | Matches the S1–3 convention (their CI green came from `workflow_dispatch` on the pushed session branch; `main`'s `ci.yml` is an old minimal version with no gate jobs). Branch-per-session, do not merge to `main`. Rejected: opening a PR / merging (would alter `main`, riskier, not the pipeline's pattern). |
| D-02 | **Added a `workflow_dispatch run_full_nightly` input** to force the nightly path on demand. | TECH | GitHub fires `schedule:` ONLY from the default branch, and `main` has no schedule — so the nightly chaos job can't auto-run on a session branch. The input lets the exact nightly path execute once in real CI (run 27485499676 = success), proving "nightly chaos ran once" honestly. True scheduled firing remains gated on merge-to-main (noted for S6/S8). |
| D-03 | **Branched `session-5-performance` off `cdf3dbf`, keeping `session-4-chaos` pristine at the audited tip.** | SCOPE | The integrity checkpoint audited `session-4-chaos @ cdf3dbf` clean; pushing it unchanged + branching S5 off it preserves the audited artifact and keeps S5 work isolated (Hard rule 8). |
| D-04 | **Methodology CO ruling: JMH `SampleTime` is structurally CO-free for in-process synchronous reads** (with two caveats: sub-sampled tail bins → report tail Cnt; unloaded-service-time scope). | TECH | Independent `review-architect` sub-agent ruled it SOUND-WITH-CAVEAT; both caveats folded into methodology §3a. Rejected: treating in-process per-op latency as CO-hazardous (there is no arrival schedule to skip). |
| D-05 | **RR-009 resolved by RELABEL + a `getInto` content test, not by wiring `getInto` into the production read path.** | SCOPE | "Relabel the claim" is one of RR-009's two offered fixes and avoids a hot-path change at the tail of a long context (charter §3.4 caution). Closed the real sub-gap (getInto's missing copy-correctness test) instead, red/green proven. Rejected: rewiring the hot read path to `getInto` (riskier; `getInto` was under-tested). |
| D-06 | **GC collector = generational ZGC (ADR-0041).** | TECH | Data-driven bake-off (G1/ZGC/Shenandoah, same workload/heap): ZGC max STW pause 0.045 ms vs G1 20.6 ms (≈450×) vs Shenandoah 0.905 ms, at no throughput cost. Pause **distribution** captured per collector, not just a max ("no ZGC-because-low-pause without the histogram"). |
| D-07 | **GC bake-off heap fixed at `-Xms96m -Xmx96m`.** | TECH | At the box-bound op rate the alloc rate is ~5–9 MB/s, so a large heap yields 0–2 GCs/fork — too sparse for a pause *distribution*. A small fixed heap applies identical pressure to all three collectors and forces 7–55 GC cycles, populating the distribution. It is a *comparative* bake-off; absolute heap/throughput at fleet scale are ENV-BLOCKED. |
| D-08 | **10k/s sustained + 100k/s burst → ENV-BLOCKED (manifest M-9/M-10), not faked.** | SCOPE/HONESTY | The 2-vCPU box hosting 3 node JVMs + a driver saturates at ~125–172 commits/s (write threads starve Raft heartbeats → leadership step-down) — ~60–80× below 10k/s. The consensus *mechanism* handles >>10k/s (RaftCommitBenchmark ~815k/s in-memory) = LOCAL-VERIFIED; the end-to-end sustained rate is box-bound = ENV-BLOCKED. The a-priori methodology §1 "fully local" classification was *corrected by measurement*, not defended. Rejected: reporting a throttled ~62/s run as if it met 10k/s. |
| D-09 | **§11 backpressure doc-vs-code gaps → FILE RR-110, do NOT unilaterally relabel the §11 policy table.** | SCOPE | Charter §8 says S5 FILES threshold mismatches; whether to relabel §11 to the as-built single-bound reality OR implement the missing ladder (Retry-After, hysteresis, apply-lag-503, ReadIndex-shed) is a §11-policy decision owned by S6 operability. Rejected: rewriting architecture.md §11 unilaterally. |
| D-10 | **D's reconnect-storm "distribution at fleet sizes" via the in-sim `OverloadChaosTest` re-run**, not the live cluster. | TECH | The in-sim path is deterministic and leadership-stable (no 2-vCPU CPU-starvation ceiling), so the recovery-tick distribution IS locally measurable — unlike the live throughput ladder (M-10). |
| D-11 | **Used `Agent` sub-agents (per the charter team roles), NOT the `Workflow` multi-agent tool.** | SCOPE | No ultracode opt-in / explicit workflow request from the user; the charter's "spawn a fresh opus sub-agent" maps to the `Agent` tool. Each measurement workstream was delegated to a background opus sub-agent owning the box (one at a time), with lead spot-checking the raw captures (verify-don't-trust). |
| D-12 | **24 h soak: launch + checkpoint + resume; run at a box-sustainable rate, labeled by its real duration.** | SCOPE | Per charter §14.6 — a 24 h wall-clock soak cannot complete in-session. The soak runs at the box-sustainable ~150/s (M-9), which still surfaces leaks/drift (monotonic heap/FD growth is rate-independent); its real duration is reported honestly (never a short run called a 24 h soak), with the full-duration/production-rate run handed to resume on real infra. |

**Verification discipline applied throughout:** every delegated measurement's raw capture was lead-spot-checked against the agent's summary before acceptance (e.g. Workstream A's `getHitWithCursor@10⁶` p99 1.60 µs / p999 32.4 µs confirmed in `wsA-jmh-sample-tails.txt`; the GC bake-off ZGC sub-0.2 ms vs G1 20.6 ms confirmed in the `-Xlog:gc*` logs). No number was accepted on an agent's say-so alone.
