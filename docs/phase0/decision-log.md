# Phase 0 — Decision Log

Per session §2 (autonomy directive) and §8.8: every self-resolved technical decision and every
conservative-default scope decision is recorded here for retroactive veto. Newest first.

| ID | Date | Type | Decision | Rationale | Reversible? |
|---|---|---|---|---|---|
| D-015 | 2026-06-21 | technical + verification (Stage 2 — M1) | **M1 — per-owner tick generalization at N>1; owner-isolation proven.** `ConfigdServer`'s consensus tick is generalized from the Stage-1B single owner[0] schedule to a per-owner loop: each owner[i] runs `driver.tickOwner(i)`/`maybeCompactOwner(i)`, which filter to exactly owner-i's groups via `floorMod(gid,N)`. The SINGLETON housekeeping (H-3 scrape of group 0 + co-tenant riders + ~10s snapshot-compact) is confined to owner[0], **split around `maybeCompactOwner`** so the N=1 operation order (tick→scrape→compact→riders) is ORDER-EXACT to the deleted Stage-1B schedule. Behaviourally exact at N=1; at N>1 in production (single group 0, owned by owner[0]) owners 1..N-1 tick zero groups until Phase-1 sharding. **Co-tenant riders kept on owner[0]** rather than moved to a dedicated housekeeping executor — recon (D-013) + the red-team confirm they hold NO `RaftNode` reference (`configd-distribution-service`/`observability` don't even depend on `configd-consensus-core`), so owner[0] is a safe singleton home; the housekeeping-executor extraction is deferred (cleanliness, not correctness). Owner-isolation proven by `OwnerIsolationMultiOwnerTest` (N=3, six groups across three owners): clean run = zero net fires + commitIndex GROWTH on a group bound to EVERY owner (non-vacuous on all three); a cross-group access (group B's entry points on group A's REAL owner thread) trips the **per-node** net (proving the guard is per-node, not per-pool) with a control showing on-owner access stays silent. Net re-proven non-vacuous for the cross-group class (neuter→RED→revert→green, captured in `docs/phase0-B-stage2/captures/m1-owner-isolation-net-catch.md`). Commits `a3c5b7f` + `2666f01`. | Staging logic (§3): generalize per-owner ticking with STATIC assignment BEFORE rehoming (M2) — isolate "does per-owner ticking work" from "does handoff work". Four-way verified: implementer + diff-review (SOUND) + independent re-run (CONFIRMED-GREEN) + red-team. The red-team found the clean-run liveness non-vacuity was setup-satisfiable (self-election commits the leader no-op BEFORE any `tickOwner` runs, so a gutted `tickOwner` still passed) — fixed to assert commitIndex GROWTH past a post-setup baseline + added a deterministic per-owner-filter unit test; fix red/green-proven + second-agent REPLAY-CONFIRMED (§6). Also re-anchored the RR-005 source-grep guard (`ConfigdServerTest`), which was satisfied only by an incidental comment (`driver.maybeCompact(`) → re-pointed at the real per-owner call `driver.maybeCompactOwner(owner,`. S2–S4 sim surface 174/0 + server 165/0 green. | Yes (revert to `3ae0bcf`; the production change is `ConfigdServer`-only). |
| D-014 | 2026-06-21 | scope (stop / continue) | **Checkpoint at the complete, adversarially-verified Stage 1 seam (`a3c8349`); Stage 2 (N>1 + coalesced heartbeats + H-4 co-tenant rehoming) continues fresh.** After H-3 (CLOSED) + the baseline gitleaks repair + Stage 1 (the R-01 deletion @ N=1 — the project's most dangerous change, verified by the implementer + a line-by-line lead review + an independent re-run + an adversarial red-team that could not break it), the conservative default (§3) is to checkpoint at this clean / committed / pushed / net-green / adversarially-verified seam rather than open Stage 2 without the context budget to give it the SAME rigor. Stage 1 is independently merge-ready; Stage 2 is designed (`stage1-design.md §7`, `handoff.md §3`) and ready to resume. `docs/phase0-B/handoff.md` written. | The brief is emphatic: "there is no glory in finishing fast here"; "slow, gated, verified." §8.7 favours a clean verified seam over a rushed multi-stage push. Stage 2 (owner-isolation proof + coalesced heartbeats + the S2–S4 multi-group re-run) deserves Stage-1-level verification, which needs fresh budget — not a context-starved pass on the consensus core. | n/a (sequencing; Stage 2 resumes from `a3c8349`). |
| D-013 | 2026-06-21 | technical + scope (Stage 1B — R-01 DELETED @ N=1) | **R-01 (the single `configd-tick` thread) is DELETED**; consensus now runs through the owner-executor pool. Approach: "replace `tickExecutor` with `ownerExecutor(gid)`" at N=1 — `new OwnerExecutorPool(Integer.getInteger("configd.raft.ownerPoolSize",1))`; bind owner[0] as the FIRST task (H-6, before the inbound handler is published + before the tick schedule); the tick lambda runs on owner[0] calling `tickOwner(0)`/`maybeCompactOwner(0)`; the 4 marshalling hops (inbound / propose / read double-hop / flush) retarget to `driver.ownerExecutor(gid)`; H-1 ((index,term) captured inside the task) + H-2 (double-hop) preserved; per-owner shutdown. The co-tenant riders (watch/plumtree/propagation/compactor) + the H-3 scrape stay on owner[0] at N=1 (behavioural equivalence) — decomposition to a housekeeping thread is **Stage 2**. Implemented by a worktree-isolated consensus-core-engineer sub-agent (§5 agent team; §8.7-safe — the worktree either returns a COMPLETE deletion or reverts, so the main tree never holds a half-deleted R-01), cherry-picked as `682cbcf`. **Permanent net-catch proof added** (`OwnerNetCatchesOffOwnerInboundTest`): drives `driver.routeMessage` off-owner through the production `raftInboundHandler` seam with a production-mode `InvariantMonitor` → `raft_owner_thread` counter ≥ 1 (the §6.3 "net still catches under the pool" requirement, made permanent), plus a clean-path-0 and a wired-server `/metrics`-0 boot guard. | The deletion is the change most likely to introduce a silent race; staged at **N=1 first** (§2) to isolate the threading-model change from multi-group, kept behaviourally exact-R-01, behind the net (re-proven to catch). Lead reviewed the full diff line-by-line (H-6 bind ordering, all 4 hops, shutdown — correct), is independently re-verifying (net + server suite + sim seed-sweep), and an adversarial pass follows before the Stage-1 checkpoint. | Yes (revert `682cbcf` → `4a1e3da`, the additive Stage-1A seam). |
| D-012 | 2026-06-21 | verification (H-3 adversarial replay) | **Independent adversarial review (isolated worktree) confirms H-3 CLOSED** — could not break the safety; all five mandate items PASS with genuine red/green: removing a guard → the net-coverage test goes RED; no-op'ing `publishMonitorView()` → the macro test's vacuity guard goes RED; the jcstress control `PerFieldPublishCanTear` surfaced real TORN in every fork (0.06–1.08%) while gated `PublishedSnapshotNeverTears` stayed 100% clean; the ONLY prod `RaftNode`-state reader is the `ConfigdServer` scrape; `AdminService` provably unwired (zero `implements ClusterStateProvider`); HTTP `leaderId/role` reads are S-set volatile; the `clusterConfig` `peersCache` race is real. **Caveats:** **C1** (the no-tear proofs can't catch a future regression making `RaftMetrics` mutable / `monitorView` non-volatile) — **ADDRESSED** by two structural-invariant guards in `RaftMonitorViewConcurrencyTest`; **C2** (`monitorView` non-null relies on safe publication of the node reference) and **C3** (the scrape still reads `MultiRaftDriver.groups`, a plain `HashMap`, off-owner — this is **H-5**, on the live scrape path) — **carried to Stage 1** (`stage1-design.md §5.1`). | The brief mandates second-agent replay and "the VERIFICATION is reviewed harder than the change." The replay confirmed the closure AND surfaced C3 — the one live-scrape-path off-owner read outside H-3's charter that Stage 1 must close before owners bind. Review: `docs/phase0-B/reviews/h3-adversarial-review.md`. | n/a (verification gate). |
| D-011 | 2026-06-21 | technical (H-3 CLOSED) | **H-3 (the monitoring-read hazard) resolved by an owner-published immutable `RaftMetrics` snapshot:** a `volatile monitorView` republished at the end of every `tick()`; the one LIVE production reader (the `ConfigdServer` tick-loop scrape) retargeted onto `monitorView()`; and the five formerly-unguarded accessors (`currentTerm`/`votedFor`/`log`/`transferTarget`/`clusterConfig`) then guarded with `assertOwnerThread()`. Recon narrowed the live prod hazard to that single scrape site — `AdminService.ClusterStateProvider` is unwired (latent, recorded), and `clusterConfig` is additionally unsafe off-owner via a lazy `peersCache` HashMap. The mechanism never tears/partial, never blocks the owner, ≤ 1 tick stale. | §1.2/§4 require an explicit H-3 safety design before the R-01 deletion merges. One coherent published snapshot beats per-field volatile (cross-field coherence + the Map/live-RaftLog cases) and **strictly strengthens the net** — the blind spot becomes guarded. Evidence: jcstress `PublishedSnapshotNeverTears` (+ `PerFieldPublishCanTear` tearing control) + `RaftMonitorViewConcurrencyTest`; consensus-core 338 + config-store/server/testkit green; design `docs/phase0-B/h3-monitor-view-design.md`. | Yes (impl); the design is the spec. |
| D-010 | 2026-06-21 | scope (supersedes D-002 for the R-01 deletion) | **Workstream B works on branch `phase0-B-rethreading`; `main` stays pinned at the verified baseline `df3f3b7`.** The R-01 deletion + owner-executor pool merge to `main` only at the §6 merge gate (CI green on df3f3b7 + the net re-proven to catch a race under the new model + H-3 closed + S2–S4 green at the merged stage). | The B brief (§1.1 "the deletion does not merge onto an unverified base"; §6 "merges to main only when …") is a stronger, irreversibility-specific constraint than D-002's commit-to-main norm — which governed the *additive* A net, not the *dangerous* R-01 deletion. Keeping `main` at the verified baseline makes the dispatched baseline CI (run 27912755341) unambiguous and forbids stacking the deletion on an unverified base. | Yes (fast-forward `main` at the gate). |
| D-009 | 2026-06-21 | milestone (Workstream A CLOSED) | **The verification net is complete.** Review-H2 closed: `assertOwnerThread()` now guards all 14 mutator/callback O entry points (core 7 + the 7 orphaned riders transferLeadership/triggerSnapshot/isReadReady/completeRead/whenReadReady/cancelCommitOutcome/proposeConfigChange); the off-owner fire-test drives all 14 and each trips. Added the JMM micro-race (`configd-jcstress` `RaftOwnerThreadGuardTest`: no-false-negative gated/clean + unbound-races forbidden-control at ≈34%) and the sim integration (`ClusterHarness`/`AdversarialSim` bind owners; `raft_owner_thread` rides the throwing checker; 20,001 seed-sweep + adversarial schedules green; `OwnerThreadSimIntegrationTest` proves an off-drive-thread access fails the seed). `bindOwnerThread()` made public (wiring + sim need it). **Scope call:** the read-only O accessors (currentTerm/votedFor/log/transferTarget/clusterConfig) are deliberately left unguarded — they are the H-3 monitoring-read hazard (resolved by a metrics snapshot, not a tripwire), owned by Workstream B; guarding them now would conflate review-H2's mutator net with H-3. Per the prime directive this CLOSES Workstream A: the net exists and is proven to catch a race at the unit, memory-model, and randomized-schedule levels — Workstream B (re-threading) opens in a fresh session behind this net. | The net must be complete BEFORE B deletes R-01, because B's safety is only as good as the net's coverage. Three independent levels (macro stress, jcstress JMM, sim) each shown to catch an injected off-owner access; binding active across 20k+ schedules with zero spurious fire proves the contract holds for the current single-owner access pattern. | n/a (gate, not a reversible decision) |
| D-008 | 2026-06-21 | milestone (verification gate) | Workstream A verification machinery is built and PROVEN: the inert-until-bound owner-thread tripwire is on `RaftNode`'s core O entry points, and `RaftNodeConcurrencyStressTest` both (a) runs the marshalled concurrent workload green (non-vacuous — proposals commit) and (b) catches an injected missed-marshalling (captured red — [evidence](captures/harness-catches-injected-race.md)). Per the prime directive (§1.1/§8.1) this is THE gate: Workstream B re-threading may now proceed, each step behind this harness — and not before. Baseline `e31864b` is fully green in CI (all gates 1–7). | The S1 vacuous-tests lesson: a verification suite is trustworthy only once shown to catch the failure it guards. The injected-red capture is that proof. | n/a (gate, not a reversible decision) |
| D-007 | 2026-06-21 | correctness (scope) | Repaired a PRE-EXISTING baseline CI failure outside Phase 0 scope: `TcpRaftTransport.closeQuietly(Socket)` NPE'd on a null socket in `tearDown` (a handshake-failed `PeerConnection` holds a null socket), reddening `build-and-test`/gate-1/gate-2 for ≥6 commits. Fixed minimally — null guard mirroring the already-guarded `closeQuietly(AutoCloseable)` sibling. Red→green verified on JDK 25; second-agent replayed. Evidence: [baseline-ci-transport-closequietly.md](captures/baseline-ci-transport-closequietly.md). | The operator gated harness work on a green baseline (RR-107: local-green ≠ CI-green). A deterministic, contained NPE in a cleanup helper is the safest possible unblock and directly serves that gate. Not a consensus-core change; zero TLS/security behavior change. | Yes (revert the guard). |
| D-006 | 2026-06-21 | technical | The R-01′ owner-thread tripwire reuses `RaftNode`'s existing `InvariantChecker` seam (`:219–224`) — throws in test/sim, metric+SEVERE in prod — mirroring `ConfigStateMachine.assertOwnerThread()` (RR-029/W-1). **Correction:** `RaftNode` has NO `metrics` field (verified `:240–364`); route the tripwire purely through `invariantChecker.check("raft_owner_thread", …)`, whose documented contract (`:214–217`) is already "throw in test, increment a metric in prod." | The seam already drives the 9 in-node invariants; reusing it means zero new verification plumbing and identical test/prod duality. | Yes (impl detail). |
| D-005 | 2026-06-21 | scope | M1 artifacts (4 `adr-multiraft-*`/`adr-throughput-target` ADRs + `docs/multiraft/`) are currently **uncommitted** (`git status ??`). Will bring them into git at the first clean seam to prevent loss; immutable thereafter (§7). | They are "accepted/immutable" per the brief but at risk while untracked. Committing them is the conservative, loss-preventing default. | Yes. |
| D-004 | 2026-06-21 | sequencing | Wrote `threading-contract.md` (Workstream A.3) **before** building the concurrent harness (A.1), though §4 numbers the harness first. | The contract is the specification the harness *asserts*; it is explicitly "written before the [re-threading] code." Writing it first de-risks the harness design. **Hard constraint preserved:** no Workstream B re-threading is blessed until the harness exists and is proven to catch an injected race. | n/a |
| D-003 | 2026-06-21 | technical | The Workstream C decision-gate **number** (single-group throughput with the fix) is a **dedicated-hardware lane item**, not measurable in this 2-vCPU codespace (~136–172 commits/s ceiling, ENV-BLOCKED). Here: build + smoke the measurement harness only. | Matches the S5/S7.5 reality and the session's own §6 caveat ("otherwise local with the honest caveat that a dedicated-hardware confirmation is a manifest item"). | n/a |
| D-002 | 2026-06-21 | scope | Commit to `main` at clean, pushed seams (per this project's established norm) rather than branch-per-change. | Every prior session and the current `HEAD` history commit directly to `main`; the session brief itself directs "checkpoint at a clean, committed, pushed seam." Branch-per-change would diverge from the repo's entire history. | Yes. |
| D-001 | 2026-06-21 | scope | Read "gates 1–7.5 stay green" as **gates 1–7 + the S7.5 perf/soak surface**. There is no `gate-7.5.sh`. | Recon: the gate chain is `gate-1.sh … gate-7.sh`; "7.5" exists only as `docs/session-7.5/` + `perf/` scripts. The cumulative correctness chain is 1–7; S7.5 is the perf/soak work layered on. | n/a |

## Standing assumptions (carried, not yet decisions)

- **No live soak in this environment.** `ps` shows no `java`/`mvn` soak process here; the S7.5
  soak-trend commits were recorded against a remote perf box (m6id.4xlarge). A soak may still be
  live on that hardware — irrelevant to dev work in this repo, but I will not assume this box is
  the perf box.
- **Build is Maven multi-module**, 14 `configd-*` modules; running the full gate chain here is
  multi-hour. Discipline: keep the build + touched-module tests green as I go; run targeted gate
  steps (gate-1 build, consensus-core tests, jcstress curated subset) at seams; defer the full
  chain to CI/nightly; **report honestly what was run vs. deferred.**
