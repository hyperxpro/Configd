# Code-Archaeologist Findings — Session 1 Ground Truth

> **Role:** code-archaeologist (senior Java systems). **Scope:** all 12 modules, src/main + src/test,
> branch `session-1-ground-truth` @ `423a654`. **Method:** read-only (grep/find/git/Read), no Maven run.
> **Rule applied:** every prior-audit claim (`docs/STATE-OF-REALITY.md`, `docs/READINESS-LEDGER.md`) that
> this sweep touches was re-verified against the CODE and is marked **corroborated** / **contradicted** /
> **superseded-by-fix**. Identification only — nothing repaired.
>
> Severity: **P0** safety/data-loss/cannot-run · **P1** correctness risk or hot-path constraint violated ·
> **P2** quality gap · **P3** polish.
>
> Note on baseline drift: the checked-out branch already contains the A1 (R-01 event-loop serialization),
> A2 (invariant net ON, spec de-vacuum), and A3-B (linz harness) fix commits. Several STATE-OF-REALITY
> claims are therefore true-at-baseline but **superseded by fix** in current code; marked as such.

---

## 1. Markers & stubs

| ID | file:line | Sev | Description | Blast radius | Prior-audit |
|---|---|---|---|---|---|
| CF-01 | `configd-consensus-core/src/test/.../WalWireCompatStubTest.java:53,69,72` and `SnapshotWireCompatStubTest.java:52,69,72` | P3 | Only TODOs in the whole tree: `TODO(R-005)` wire-compat stubs whose enabled twin would `fail("...not yet wired up")`. Honest, guarded stubs. | None today; wire-format compat is unguarded until first version bump. | **Corroborates** "2 honest @Disabled stubs". |
| CF-02 | (whole tree) | — | **Clean otherwise**: zero TODO/FIXME/HACK/XXX in src/main; zero `UnsupportedOperationException`-throwing stubs in src/main (the one catch at `SigningKeyStore.java:145` is a documented best-effort POSIX-perms ignore, see CF-38); every src/main `return null` inspected — all are legitimate sentinel returns (`FileStorage.get`, `RaftLog.entryAt`, `WatchCoalescer.flush`, `HyParViewOverlay.randomActive`, etc.), none is a not-implemented shortcut. | — | — |

## 2. Disabled-test graveyard

| ID | file:line | Sev | Reason string | Blast radius |
|---|---|---|---|---|
| CF-01a | `WalWireCompatStubTest.java:58` | P3 | `@Disabled("Stub: enable on first version bump (R-005). ...")` | WAL byte-format regressions undetected until v2. |
| CF-01b | `SnapshotWireCompatStubTest.java:58` | P3 | `@Disabled("Stub: enable on first version bump (R-005). ...")` | Snapshot byte-format regressions undetected until v2. |

These are the only `@Disabled`/`@Ignore` in the repo (matches the suite's "2 skipped").

## 3. Dead / orphaned code

Method: for each class, grep all references across src/main + src/test, excluding the class's own file.

| ID | Class / package | Sev | Evidence | Blast radius | Prior-audit |
|---|---|---|---|---|---|
| CF-03 | `replication/ReplicationPipeline` | P2 | Referenced only by `ReplicationPipelineTest` | "Replication engine" headline is one HashMap (`MultiRaftDriver`); no live log-shipping. | **Corroborates** orphan claim. |
| CF-04 | `replication/SnapshotTransfer` | P2 | Only `SnapshotTransferTest` | Chunked snapshot transfer exists only on paper; live InstallSnapshot is single-blob (see CF-42). | **Corroborates**. |
| CF-05 | `replication/FlowController` | P2 | Only `FlowControllerTest` | No live flow control / backpressure on replication. | **Corroborates**. |
| CF-06 | `replication/HeartbeatCoalescer` | P2 | Only `HeartbeatCoalescerTest` | Multi-group heartbeat coalescing never used (one group). | New (same family). |
| CF-07 | `distribution/CatchUpService` | P2 | Referenced by **nothing at all — not even a test** | Fully dead file; edge catch-up path absent. | **Corroborates + sharpens** (prior audit said "only self-tests"; in fact zero refs). |
| CF-08 | `api/AdminService` | P2 | Referenced by **nothing at all** (no instantiation, no test) | No admin surface; also makes `RaftNode.transferLeadership` production-unreachable (CF-15). | **Corroborates + sharpens** (zero refs, not just "no non-test instantiation"). |
| CF-09 | `io.configd.edge` package (9 classes) | P2 | Zero src/main references outside configd-edge-cache; only `ConsistencyPropertyTests`/`EndToEndTest` (testkit tests) import it | Every edge-staleness / RYW / monotonic-read guarantee rides on code no server ever loads. | **Corroborates**. |
| CF-10 | `distribution/PlumtreeNode` | P2 | Constructed + `tick()`ed (`ConfigdServer.java:333,561`) but `broadcast()` called only in `PlumtreeFanOutBenchmark.java:62/76`; `drainOutbox()` never called in src/main → even if broadcast ran, **no message could ever leave the node** (outbox is an unbounded `LinkedList`, `PlumtreeNode.java:88,233`). Startup banner `ConfigdServer.java:855` prints "Distribution : Plumtree + HyParView **(wired)**" — operator-facing misstatement. | Inert protocol burning a HashSet alloc/10ms (CF-26); deploy-shaped false promise. | **Corroborates** ("benchmark-only broadcast"); banner contradiction is new. |
| CF-11 | `distribution/HyParViewOverlay` | P2 | Constructed (`ConfigdServer.java:334`), listener wired (`:338-344`), but no join/shuffle/neighbor message ever reaches it (its `handle*` methods and `drainOutbox` have zero src/main callers) → active view永 empty → listener never fires → Plumtree peer sets stay empty. | Membership overlay is decorative. | **Corroborates**. |
| CF-12 | `distribution/FanOutBuffer` | P2 | Appended at `ConfigdServer.java:360`; **no src/main caller of `deltasSince`/`latest`/`canReplayFrom`** (only `FanOutBufferTest`, `ConfigdServerTest`). Growth policy: bounded ring, capacity 10,000, evicts oldest (`FanOutBuffer.java:38-40`) — NOT a leak. | Write-only sink; the entire committed-log→edge pipeline dead-ends here. | **Corroborates** "appended, never drained"; adds: bounded, so no memory risk. |
| CF-13 | `distribution/WatchService` + `SubscriptionManager` | P2 | Producer side wired (listener `ConfigdServer.java:365`, `watchService.tick()` `:560`), but no production path registers a watcher/subscriber (no HTTP watch endpoint; `SubscriptionManager.subscribe` called only from `SubscriptionMatchBenchmark` and tests) → dispatch always hits 0 watchers. | Push-notification feature unreachable by any client. | New (same family as fan-out). |
| CF-14 | `RaftNode.proposeConfigChange` (`RaftNode.java:514`) | P2 | Callers: only `ReconfigurationTest` + `CertificationTest`. No server/API path. | Joint-consensus reconfig — heavily spec'd and invariant-checked — cannot be invoked on a running system; cluster membership is fixed at boot. | **Corroborates**. |
| CF-15 | `RaftNode.transferLeadership` (`RaftNode.java:299`) | P3 | Only src/main caller is dead `AdminService` (CF-08). | No operational drain/transfer. | New. |
| CF-16 | `common/Buggify`, `BuggifyRuntime` | P3 | Zero call sites outside configd-common. | FoundationDB-style fault injection: framework present, never invoked anywhere. | New. |
| CF-17 | `common/HybridClock`, `HybridTimestamp` | P3 | Used only by `HybridClockBenchmark` (testkit). | HLC machinery unused by the product. | New. |
| CF-18 | `transport/BatchEncoder`, `transport/MessageRouter` | P3 | Only their own tests. | Transport batching/routing layers bypassed by `TcpRaftTransport`. | New. |
| CF-19 | `store/DeltaComputer`; `Compactor.getSnapshot` read side | P3 | `compactor.addSnapshot`/`compact()` are wired (`ConfigdServer.java:361,566`), but nothing in src/main ever reads the retained history (`getSnapshot` → `DeltaComputer` test-only). | Snapshot history retained (≤10 + up-to-10s of un-compacted adds) for a delta-serving path that doesn't exist. | New. |
| CF-20 | `ConfigScope` routing | P3 | `ConfigWriteService.put(key,value,scope)` carries scope, but `ConfigdServer.raftProposer` (`:712`) ignores it — everything goes to `DEFAULT_RAFT_GROUP=0`; HTTP hardcodes `ConfigScope.GLOBAL` (`HttpApiServer.java:275,301`). | GLOBAL/REGIONAL/LOCAL is API theater. | **Corroborates** "scope collapsed to group 0". |

## 4. Hot-path violations

| ID | file:line | Sev | Description | Blast radius | Prior-audit |
|---|---|---|---|---|---|
| **CF-21** | `TcpRaftTransport.java:343` (plaintext), `:324` + `:340` (TLS), `:398-413`, `:415-432`; reached from tick thread via `RaftNode` sends (`:860,941,973,1109,1154,1255,1299,1639`) | **P0** | **Timeout-less blocking connect on the consensus event-loop thread — claim VERIFIED.** `new Socket(addr, port)` (no connect timeout) and TLS `factory.createSocket(host, port)` + `startHandshake()` (no connect timeout, no `setSoTimeout`) run inside `PeerConnection.ensureConnected()`, which executes on whatever thread calls `transport.send` — and post-R-01 **every** `RaftNode` send runs on the single `configd-tick` thread (tick, inbound, propose, read are all marshalled there, `ConfigdServer.java:287-291,690,715`). Additional blocking calls reachable from the tick thread: `out.write/flush` on a full TCP send buffer while holding `sendLock` (`:402-412`); `socket.startHandshake()` (`:340`); plus `synchronized(connectionManager)` (`:311,:428` — short, no I/O). **Aggravator:** `ConnectionManager` records `markConnected/markDisconnected` but its backoff (`canSend`) is **never consulted** — the class javadoc's "exponential backoff" claim is contradicted by code — so every heartbeat toward a dead peer re-attempts a fresh blocking connect. | One unreachable peer (SYN black-hole, e.g. partition with DROP rule) stalls the tick thread for the OS connect timeout (~2min default) **repeatedly**. Because R-01 funneled everything onto this one thread, the stall takes down elections, heartbeats, CheckQuorum, all writes, and all linearizable reads on that node simultaneously. A1's fix turned a data race into a single global choke point — the recurring "verified-but-untested-integration seam", transport edition. | **Corroborates** the ~line-343 claim; severity raised by the R-01 interaction (post-baseline). |
| CF-22 | `FileStorage.appendToLog:89-114` (open+`force(true)`+close **per entry**); `RaftLog.append:282-285`; `RaftLog.rewriteWal:485-507` (O(n) appends, each fsynced) + `truncateFrom:360-368` | P1 | WAL durability is implemented as one `FileChannel.open`→write→fsync→close cycle **per log entry**, on the tick thread. A 64-entry AppendEntries batch = 64 open/fsync/close cycles inside a 10 ms tick budget; a follower-side conflict truncation rewrites and fsyncs the **entire** WAL entry-by-entry. | Tick overruns under any real write load → heartbeat/election timers driven by a stalling loop; commit latency cliff. (Durability ordering itself is correct — see CF-41.) | New; consistent with prior audit's "perf modeled, not measured". |
| CF-23 | `VersionedConfigStore.get:189-197`, `LocalConfigStore.get:119-127` | — | **Verified clean (positive finding):** stale-read path is a single volatile read + immutable-HAMT traversal; zero locks, zero CAS, zero alloc on miss, one 24-B `ReadResult` on hit; `getInto` offers zero-alloc hit. No synchronized/lock anywhere on either read path (grep-verified). | — | **Corroborates** "genuinely lock-free read path". |
| CF-24 | `ConfigdServer.java:483-510` | P2 | Linearizable read allocates `CompletableFuture` + `AtomicLong` + 3 lambdas per request and double-hops `readDispatchExecutor`→`tickExecutor`. Self-documented as the F-0022 compromise. | Violates strict "no steady-state allocation" on the linearizable-read path; GC pressure at high read QPS. | Consistent with prior audit. |
| CF-25 | `RaftTransportAdapter.send:49`, `FrameCodec.encode`, `RaftLog.serializeEntry:471-478`, `ConfigStateMachine.signCommand:574-623` (canonical re-encode + `SecureRandom` nonce per write) | P2 | Per-message/per-write allocation on the Raft hot path: every send encodes a fresh `byte[]`; every applied write re-decodes, re-encodes (canonicalize), allocates nonce + signing buffer. | Steady-state allocation in the write/replication path; contradicts the project's own zero-alloc constraint (read path honors it; write path does not). | New. |
| CF-26 | `PlumtreeNode.tick():209`, called every 10 ms from `ConfigdServer.java:561` | P3 | `new HashSet<>()` per tick for a protocol that can never carry a message (CF-10/11). | Pointless steady-state allocation on the tick thread. | New. |
| CF-27 | `TcpRaftTransport.java:211,251-260,277-293`; `RaftTransportAdapter.java:68`; `RaftNode.java:483,1065,1257,1301` | P2 | Error-path logging is raw `System.err.println` from the tick/IO threads (synchronized stream, invisible to the JUL pipeline the H-009 fix established for the tick loop). No per-request INFO+ logging on happy paths (verified clean); no reflection anywhere in src/main (verified). | A flapping peer produces a stderr storm that itself blocks the tick thread; transport errors invisible to log aggregation/metrics. | New; complements prior H-009 finding. |
| CF-50 | `RaftNode.tickElection:760-774` / `tickHeartbeat:776-791` / `resetElectionTimeout:1648-1651` vs `RaftConfig` (`electionTimeoutMinMs=150, MaxMs=300, heartbeatIntervalMs=50`) vs `ConfigdServer.TICK_PERIOD_MS=10` | P1 | **Tick/millisecond unit mismatch.** Fields named `...Ms` are counted in **ticks** (`electionTicksElapsed >= electionTimeoutTicks` where `electionTimeoutTicks = electionTimeoutMinMs + rand`; `heartbeatTicksElapsed >= config.heartbeatIntervalMs()`); the server ticks every **10 ms** (and `RaftNode` javadoc says "e.g., every 1ms"). Effective election timeout = **1.5–3.0 s** (10× documented), heartbeat = **500 ms**, CheckQuorum step-down ≈ 10× slower. Ratios preserved, so it functions — but every documented timing number is wrong at runtime. | Failover takes seconds, not the documented 150–300 ms; SLO math and the linz schedule windows built on documented timings are computed against a 10×-slower reality. | New — not in prior audit. |

## 5. Concurrency smells

| ID | file:line | Sev | Description | Blast radius | Prior-audit |
|---|---|---|---|---|---|
| CF-28 | `ConfigdServer.java:292` (tickExecutor) + `:690` (inbound enqueue) + `:715` (propose enqueue) | P2 | The single tick executor's queue is unbounded; inbound Raft traffic and HTTP proposes enqueue without any limit ahead of the periodic tick task. | Message flood from a peer → unbounded heap growth + tick starvation (head-of-line over the DelayedWorkQueue). No backpressure between transport and consensus. | New (consequence of post-baseline R-01 fix). |
| CF-29 | `ConfigdServer.raftInboundHandler:690` | P2 | The marshalled inbound task `raftExecutor.execute(() -> driver.routeMessage(...))` has **no try/catch**. A throw inside `handleMessage` (e.g. `UncheckedIOException` from WAL append on disk-full, `FileStorage.java:111-113`) is captured by the executor's internal FutureTask and **silently discarded** — no log, no metric, no thread death. (Contrast: tick path has the H-009 handler `:568-577`; propose path completes exceptionally `:716-720`.) | Disk-full follower silently stops persisting/acking while looking alive — a mute zombie. Same silent-failure mode H-009 fixed for the tick loop, reintroduced on the inbound path by the R-01 patch. | New. |
| CF-30 | `ConfigStateMachine.java:72-107` (`lastSignature/lastEpoch/lastNonce/sequenceCounter/signingEpoch` non-volatile, public getters `:656-697`) | P3 | W-2: fields written on the apply (tick) thread, exposed via getters. Post-R-01 all production readers (the `ConfigdServer.java:347-362` listener) run on the same tick thread → safe by program order. Race exists only for off-thread getter callers (today: tests). | Latent until a future seam reads them off-thread. | **Corroborates** W-2; downgraded by R-01 fix. |
| CF-31 | `ReadResult.value():56-58` (no clone; cf. `VersionedConfigStore.get:196` passing `vv.valueUnsafe()`) | P3 | R-1: `ReadResult.value()` hands out the HAMT's live internal `byte[]`; sibling paths clone. Sole src/main consumer is `HttpApiServer.java:251` (`os.write(value)`, read-only). | Any future caller that mutates the array corrupts the store in place, bypassing MVCC. | **Corroborates** R-1 (still unfixed). |
| CF-32 | `TcpRaftTransport.close():198` (`shutdownNow`, no `awaitTermination`); `HttpApiServer.java:85` (VT executor never shut down on `stop`) | P3 | Executor teardown is fire-and-forget; in-flight handler threads may outlive `close()`. | Test/embedded restarts can briefly double-handle frames; minor thread leak on stop. | New. |
| CF-33 | `FanOutBuffer.deltasSince:43-54` | P3 | `head`/`tail` read non-atomically; writer can lap a reader mid-scan, returning slots already overwritten by newer deltas attributed to the old window. Harmless today (no src/main reader, CF-12) but the class is advertised as the future fan-out source. | Wrong/duplicated deltas to edges the day B-phase wires a drain. | New; flags the B-phase seam the ledger warns about. |
| CF-34 | `VersionedConfigStore` writes (`put/delete/applyBatch/restoreSnapshot`) | P3 | W-1: single-writer precondition is documented (`:22-29`) but unenforced — no owner-thread assertion. Post-R-01 the only writer is the tick thread. | Silent corruption if a second writer is ever wired (the recurring seam). | **Corroborates** W-1 (still unguarded). |
| CF-35 | (verified clean) | — | No `Thread.sleep`-as-synchronization in src/main of server modules (sleeps exist only in the configd-linz fault harness, by design); every `catch (InterruptedException)` in src/main re-interrupts; no double-checked locking found; VT pinning: the two `synchronized(connectionManager)` blocks contain no I/O; HTTP handlers (virtual threads) block only on futures/locks, not in synchronized regions. | — | — |
| (xref) | `RaftNode` thread-confinement | — | **R-01 status re-verified against code:** inbound (`ConfigdServer.java:316,690`), propose (`:432,715`), read (`:491`), tick (`:556`) all funnel through `tickExecutor`; `RaftInboundMarshallingTest` exists. No remaining off-thread `RaftNode` mutator found by sweep. | — | STATE-OF-REALITY risk #1 **superseded by fix** — corroborates ledger's R-01 CLOSED (statically). |

## 6. Error-handling hazards

| ID | file:line | Sev | Description | Blast radius |
|---|---|---|---|---|
| CF-36 | `RaftTransportAdapter.registerInboundHandler:67-69` | P3 | `catch (Exception)` on inbound decode → `System.err` only; frame dropped with no metric. | Persistent codec mismatch from one peer is invisible except to stderr. |
| CF-37 | `TcpRaftTransport.acceptLoop:208-213`, `handleInboundConnection:285-295`, `closeQuietly:370-378` | P3 | IOExceptions logged to stderr and swallowed; close errors fully ignored. Reasonable for a transport, but no counters → flapping invisible to `/metrics`. | Ops blindness on transport health. |
| CF-38 | `SigningKeyStore.java:140-146` | P3 | `UnsupportedOperationException` ignored when chmod-0600 of the Ed25519 signing key fails (non-POSIX FS) — documented best-effort. | Signing key may rest world-readable on non-POSIX volumes. |
| CF-39 | `TcpRaftTransport.handleSendFailure:308-314` | P2 | Send failure closes the connection and silently drops the frame — no retry, no metric, and (per CF-21) no backoff consulted on the next send. Raft tolerates message loss, but the *loss rate* is unobservable. | Sustained partial connectivity looks identical to healthy idle in metrics. |
| CF-40 | `FileStorage.readLog:122-134` (`ByteBuffer.allocate((int) fileSize)`) | P2 | Whole WAL slurped into one heap buffer with an **int-cast file size**: a WAL ≥ 2 GiB (reachable — CF-43 says it never shrinks) yields a negative/truncated allocation → boot crash or silent partial replay. | Node restart fails exactly when the log is largest; recovery time O(history) even below the cliff. |
| (xref CF-29) | inbound task throw swallow | P2 | See §5 — the worst error-handling hazard found. | — |

## 7. Durability (WAL / term / vote / snapshot) — full chain audit

**CF-41 — fsync-before-ack VERIFIED on the live Raft paths (positive finding; contradicts a P0 hypothesis).**
Call chains, all synchronous on the tick thread:

- **Follower append:** `RaftNode.handleAppendEntries:819` → `RaftLog.appendEntries:313` → `append:282` → `Storage.appendToLog` → `FileStorage.java:110 channel.force(true)` — completes **before** the success response at `RaftNode.java:860`. Reject paths (`:800,:821`) write nothing, reply immediately — correct.
- **Vote:** `handleRequestVote:938 durableState.vote(...)` → `DurableRaftState.persistValues:128-134` → `FileStorage.put:54-70` (temp file, `force(true)`, `ATOMIC_MOVE`, directory `sync()`) — **before** the grant is sent at `:941`. Persist-before-in-memory-update ordering throughout `DurableRaftState` (`:70-81,:93-103,:114-119`).
- **Term bump:** `becomeFollower:1046`, `startElection:1125`, `startPreVote:1086` all persist via `setTerm/setTermAndVote` before mutating memory or sending.
- **Leader propose:** `propose:285 log.append` (fsync) before `broadcastAppendEntries:286`.
- PreVote (`:973`) correctly persists nothing (spec-compliant: no state change).

No deferred/async/grouped fsync exists anywhere — durability is correct but maximally expensive (CF-22).

| ID | file:line | Sev | Description | Blast radius | Prior-audit |
|---|---|---|---|---|---|
| **CF-42** | `RaftNode.triggerSnapshot:339-347` (`latestSnapshot` = **in-memory only**) + `RaftLog.compact:399-437` (WAL entries ≤ index **permanently deleted**, only 16-byte index/term meta persisted) + `handleInstallSnapshot:1500-1510` (same on follower) + **silent-skip replay**: `applyCommitted:1356-1389` advances `setLastApplied(nextApply)` even when `entryAt(nextApply) == null` (both invariant checks sit inside `if (entry != null)`) + state machine boots empty (`ConfigdServer.java:189-191`) | **P0 (latent)** | **Snapshot bytes are never written to durable storage.** After any compaction, the only copy of all state ≤ snapshotIndex is the in-process `latestSnapshot` / the live store. On restart, `RaftLog(Storage)` recovers snapshotIndex/Term and the post-snapshot entries — and replay **silently skips** the missing prefix while marching `lastApplied` forward. The node resurrects with intact `lastIndex/lastTerm` (it can still win elections) but a state machine missing every write ≤ snapshotIndex, and with `latestSnapshot == null` it would re-snapshot the **corrupt** state for followers. No error, no invariant trip. | Silent, cluster-propagatable data loss after (compaction → restart). Currently fenced ONLY by CF-43's accidental unreachability of compaction. | New — not in prior audit (it stopped at "InstallSnapshot chunking limited"). |
| **CF-43** | `triggerSnapshot` callers: only `sendInstallSnapshot:1277`; `sendInstallSnapshot` reachable only when `prevTerm == -1` (`sendAppendEntries:1230-1234`), which requires an existing snapshot — **circular**; `ConfigdServer` never calls `triggerSnapshot`/`compact` (its `Compactor` at `:566` is the unrelated in-memory store-history compactor, CF-19) | **P1** | **Raft log compaction is unreachable in the wired server** → the WAL grows without bound for the life of the deployment, and restart replays (and heap-loads, CF-40) the entire history. RaftLog's javadoc "Snapshot frequency bounds the replay cost" describes a mechanism nothing triggers. | Disk exhaustion as a slow-motion outage; multi-GiB boot (then the CF-40 2 GiB crash); also means InstallSnapshot to lagging followers can never actually occur, so a follower offline longer than... nothing — it always gets the full log replay. | New. Prior audit's "InstallSnapshot 4 MiB cap" concern is **academic** until this is fixed (the path can't execute). |
| **CF-44** | `RaftNode.propose:283-289` (ACCEPTED = local append only) → `ConfigWriteService.put:150-154` (`Accepted` javadoc: "will be replicated") → `HttpApiServer.handlePut:277-278` (**HTTP 200** "Accepted") | **P0** | **Ack ≠ commit ≠ quorum.** The client receives 200 the moment the entry is in the *leader's* fsynced log, before any replication. A leader that fails (or is partitioned) before quorum replication has its entry truncated by the next leader's conflict resolution (`RaftLog.appendEntries:333-336`) — the acked write vanishes. The local fsync does not help: durability of an *uncommitted* entry is not commitment. | Acked-write loss under leader failover — the classic Raft client-contract violation. **Corroborates ledger R-14 (OPEN).** Note: the linz harness *knows* — `ConfigClient.java:89` records every 200-PUT as `INFO ("ack != commit")` — so the green Porcupine verdict structurally **cannot** detect this loss class; nothing currently tests it. | **Corroborates** R-14. |
| CF-45 | restart replay gating: `RaftLog` ctor leaves `commitIndex=0`; store repopulates only after a new commit advances it (single node: post-election no-op commit, ≈1.5–3 s by CF-50) | P2 | After restart, the default (stale) read path serves 404/empty for *all* existing keys until the first commit round re-applies the WAL. Linearizable reads are protected (ReadIndex requires leadership + apply catch-up); stale reads are not. | Restarted node serves a total-amnesia window of seconds to stale readers; clients without `minVersion` cursors see keys blink out. | New. |

## 8. Resource lifecycle (`ConfigdServer`)

| ID | file:line | Sev | Description | Blast radius |
|---|---|---|---|---|
| CF-46 | `ConfigdServer.shutdown:614-631`, `shutdownExecutor:739-750`, hook `:597-600` | P3 | **Ordered shutdown exists and is mostly correct** (HTTP stop → read-dispatch → tick → tls-reload → TCP transport, with awaitTermination + re-interrupt). Gaps: (a) `HttpApiServer`'s virtual-thread executor is never shut down (`HttpApiServer.java:85`; `stop()` only stops the listener); (b) `tcpTransport.close()` uses `shutdownNow` without await (CF-32); (c) **start-failure paths leak**: e.g. an `HttpApiServer` constructor throw at `:537-543` aborts `start()` after `tcpTransport.start()` and executor creation with no cleanup — leaked port + 3 daemon threads in embedded/test use; (d) cleanup relies on the JVM shutdown hook — embedded callers must call `shutdown()` themselves. `FileStorage` holds no long-lived channels (per-op open/close — the flip side of CF-22), so storage leaks nothing. | Port/thread leaks in tests and embedded use; none fatal in the systemd/k8s single-process model. |

## 9. Evidence hygiene (`spec/` TLC artifacts)

| ID | artifact | Sev | Description | Prior-audit |
|---|---|---|---|---|
| CF-47 | `spec/ReadIndexSpec_TTrace_1776462705.{bin,tla}`, `spec/SnapshotInstallSpec_TTrace_1776462986/1776462993/1776463019.{bin,tla}` — all committed in the initial import `d849eb1` ("Configd", 2026-04-06 label; trace epochs ≈ 2026-04-17/18, matching committed run dirs `spec/states/26-04-17-21-5x-*`) | P2 | **Four committed TLC counterexample traces correspond to invariant FAILURES that were never documented.** `tlc-results.md`'s "Bugs Found and Fixed" section narrates **ConsensusSpec only** (its 7 traces are accounted for); no document anywhere explains what failed in ReadIndexSpec/SnapshotInstallSpec or what was changed. The traces are readable: e.g. `SnapshotInstallSpec_TTrace_1776462986.tla` ends in a state with `snapshot = (n1 :> [index 1, term 2] @@ n2 :> [index 1, term 1])` — same snapshot index with diverging terms, i.e. a SnapshotMatching-class violation — and `state = (n1 :> "leader" @@ n2 :> "leader")`. Whether these were spec bugs or real design bugs fixed silently is unrecorded; the certification narrative cites these specs as evidence without disclosing their failure history. | New specific finding; consistent with the prior audit's "stale/imported artifacts" theme but about *undocumented failures*, which the prior audit did not surface. |
| CF-48 | `spec/tlc-output.txt:3-7` | P3 | Still references `/home/ubuntu/Programming/Configd` — an artifact imported from a different machine/tree, still in-repo and historically cited as evidence. | **Corroborates** STATE-OF-REALITY §4.7 (unfixed). |
| CF-49 | `spec/states/**` (~600 committed TLC working files, incl. 4 aborted SnapshotInstallSpec run dirs within 4 minutes on 26-04-17); `spec/*_TTrace_1780770*.bin` (June 6, on disk but **untracked** — `.gitignore` `*.bin`) | P3 | Repo cruft corroborated (ledger already notes it). Sharper point: the June 178* traces are A2's *deliberate* seeded-bug counterexamples — the very evidence backing tlc-results.md's "non-vacuous (proven by a seeded buggy-action counterexample)" claim — and they are **not committed** (ignored by `*.bin`), so that claim is currently unreproducible from the repo. CI does run all three specs (`ci.yml:61-66`) — A2 ledger claim corroborated. | **Corroborates** ledger cruft note + CI claim; adds the uncommitted-evidence wrinkle. |

## Prior-audit cross-check summary

- **Corroborated (still true):** orphan set (CF-03..CF-14, sharpened for CatchUpService/AdminService); edge package isolation; FanOutBuffer write-only; `proposeConfigChange` test-only; lock-free read path; R-1/W-1/W-2 latent store hazards (unfixed, but defanged by R-01); timeout-less connect at `TcpRaftTransport.java:343` (CF-21 — worse now); scope→group-0 collapse; stale `tlc-output.txt`; 2 honest disabled stubs; ledger R-14 ack≠commit (CF-44).
- **Superseded by fix on this branch:** STATE-OF-REALITY risk #1 (RaftNode thread-unsafety — R-01 marshalling verified in code + test exists); risk #2 (NOOP invariant checkers — real `InvariantMonitor` wired at `ConfigdServer.java:203-209,267-269`); vacuous TLA invariants + single-spec CI (A2, verified in `.cfg`/`ci.yml`).
- **Contradicted / new beyond prior audit:** `ConnectionManager` backoff never consulted (its javadoc and the transport's are wrong); snapshot-never-persisted + silent-skip replay (CF-42); compaction unreachable → unbounded WAL (CF-43); 10× tick-unit mismatch (CF-50); silent inbound-throw swallow (CF-29); undocumented ReadIndex/SnapshotInstall TLC failures (CF-47).

## Top candidate P0/P1

| Rank | ID | Sev | One-line case |
|---|---|---|---|
| 1 | CF-21 | **P0** | Timeout-less blocking `connect()`/TLS-handshake/`write()` on the single tick thread that (post-R-01) owns ticks, inbound, proposes AND reads — one black-holed peer freezes the node's entire consensus + API for minutes, repeatedly (backoff exists but is never consulted). |
| 2 | CF-44 | **P0** | HTTP 200 on leader-local append (`ProposalResult.ACCEPTED`) before quorum — acked writes are lost on leader failover; ledger R-14 corroborated; the linz checker by design cannot catch it (200-PUT recorded as INFO). |
| 3 | CF-42 | **P0 (latent)** | Snapshots live only in RAM while `compact()` deletes the WAL prefix, and `applyCommitted` silently skips missing entries on replay → restart-after-compaction = silent, election-winning data loss; fenced today only by CF-43's accidental unreachability. |
| 4 | CF-43 | **P1** | No production path ever triggers Raft log compaction → WAL grows forever; O(history) replay; with CF-40's `(int) fileSize` cast, boot crashes outright at 2 GiB. |
| 5 | CF-50 | **P1** | `...Ms` config values counted as 10 ms ticks: election timeout actually 1.5–3 s, heartbeat 500 ms — every documented timing/failover number is 10× off at runtime. |
| 6 | CF-22 | **P1** | One FileChannel open + fsync + close **per log entry** on the tick thread (64 per batch; full-WAL rewrite on truncate) — durability correct, latency budget destroyed. |
| 7 | CF-29 | **P1→P2** | Inbound-path throwables are swallowed by the executor (no log/metric): a disk-full follower becomes a silent zombie — the exact failure mode H-009 fixed for ticks, reintroduced by the R-01 patch on the inbound seam. |

*47 findings total: 3 P0 (1 latent), 4 P1, 17 P2, 21 P3, plus 2 verified-clean positives (CF-23 read path, CF-41 fsync-before-ack).*
