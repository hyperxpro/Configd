<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.configd</groupId>
    <artifactId>configd</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <modules>
        <module>configd-common</module>
        <module>configd-wire</module>
        <module>configd-transport</module>
        <module>configd-netty</module>
        <module>configd-consensus-core</module>
        <module>configd-config-store</module>
        <module>configd-edge-cache</module>
        <module>configd-observability</module>
        <module>configd-replication-engine</module>
        <module>configd-distribution-service</module>
        <module>configd-control-plane-api</module>
        <module>configd-authn-oidc</module>
        <module>configd-kms-vault</module>
        <module>configd-testkit</module>
        <module>configd-server</module>
        <module>configd-edge-node</module>
        <module>configd-linz</module>
        <module>configd-client-core</module>
        <module>configd-client-edge</module>
        <module>configd-client-http</module>
        <module>configd-client</module>
        <module>configd-conformance</module>
        <module>configd-jcstress</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>25</maven.compiler.release>
        <!-- Fixed (not date-function) so jar mtimes are deterministic; bump only on release. -->
        <project.build.outputTimestamp>2026-06-14T00:00:00Z</project.build.outputTimestamp>
        <dependency-check.version>12.1.0</dependency-check.version>
        <agrona.version>1.23.1</agrona.version>
        <jctools.version>4.0.5</jctools.version>
        <junit.version>5.11.4</junit.version>
        <jmh.version>1.37</jmh.version>
        <jcstress.version>0.16</jcstress.version>
        <!-- All network transport uses Netty 4.2; pinned for reproducibility. -->
        <netty.version>4.2.15.Final</netty.version>
        <!-- YAML parser: SafeConstructor + limits isolate to configd-server, never core modules. -->
        <snakeyaml.version>2.4</snakeyaml.version>
        <!-- Nimbus: self-contained for RS256/ES256 JDK verification (no BouncyCastle leak). -->
        <nimbus-jose-jwt.version>10.9</nimbus-jose-jwt.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.agrona</groupId>
                <artifactId>agrona</artifactId>
                <version>${agrona.version}</version>
            </dependency>
            <dependency>
                <groupId>org.jctools</groupId>
                <artifactId>jctools-core</artifactId>
                <version>${jctools.version}</version>
            </dependency>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${junit.version}</version>
            </dependency>
            <dependency>
                <groupId>org.openjdk.jmh</groupId>
                <artifactId>jmh-core</artifactId>
                <version>${jmh.version}</version>
            </dependency>
            <dependency>
                <groupId>org.openjdk.jmh</groupId>
                <artifactId>jmh-generator-annprocess</artifactId>
                <version>${jmh.version}</version>
            </dependency>
            <dependency>
                <groupId>org.hdrhistogram</groupId>
                <artifactId>HdrHistogram</artifactId>
                <version>2.2.2</version>
            </dependency>
            <dependency>
                <groupId>io.micrometer</groupId>
                <artifactId>micrometer-core</artifactId>
                <version>1.14.4</version>
            </dependency>
            <dependency>
                <groupId>net.jqwik</groupId>
                <artifactId>jqwik</artifactId>
                <version>1.9.2</version>
            </dependency>
            <dependency>
                <groupId>org.openjdk.jcstress</groupId>
                <artifactId>jcstress-core</artifactId>
                <version>${jcstress.version}</version>
            </dependency>
            <!-- Netty 4.2: native-transport classes + binaries (io_uring/epoll) managed here. -->
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-transport</artifactId>
                <version>${netty.version}</version>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-buffer</artifactId>
                <version>${netty.version}</version>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-codec</artifactId>
                <version>${netty.version}</version>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-codec-http</artifactId>
                <version>${netty.version}</version>
            </dependency>
            <!-- For IdleStateHandler (idle reaping) and SslHandler (mTLS). -->
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-handler</artifactId>
                <version>${netty.version}</version>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-transport-classes-epoll</artifactId>
                <version>${netty.version}</version>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-transport-native-epoll</artifactId>
                <version>${netty.version}</version>
                <classifier>linux-x86_64</classifier>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-transport-classes-io_uring</artifactId>
                <version>${netty.version}</version>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-transport-native-io_uring</artifactId>
                <version>${netty.version}</version>
                <classifier>linux-x86_64</classifier>
            </dependency>
            <dependency>
                <groupId>org.yaml</groupId>
                <artifactId>snakeyaml</artifactId>
                <version>${snakeyaml.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nimbusds</groupId>
                <artifactId>nimbus-jose-jwt</artifactId>
                <version>${nimbus-jose-jwt.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>net.jqwik</groupId>
            <artifactId>jqwik</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <release>25</release>
                        <compilerArgs>
                            <arg>--enable-preview</arg>
                            <arg>-Xlint:all</arg>
                        </compilerArgs>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.5.2</version>
                    <configuration>
                        <argLine>--enable-preview</argLine>
                        <systemPropertyVariables>
                            <!-- Unit/integration tests boot servers with the co-located
                                 default signing key (dataDir/signing-key.bin). The production
                                 fail-closed guard (ConfigdServer.enforceSigningKeyNotColocated) would
                                 refuse to start. Tests are a dev context, so opt out module-wide.
                                 Production runs the shaded jar WITHOUT this property and gets
                                 fail-closed; the dedicated negative test sets it false explicitly. -->
                            <configd.security.allowColocatedSigningKey>true</configd.security.allowColocatedSigningKey>
                            <!-- The shutdown() drain quiet-period is a production graceful-drain
                                 nicety (it pauses N>1 shutdown so an LB observes the readiness 503 before the
                                 listener closes). Tests are a dev context and shut servers down repeatedly, so
                                 pause 0ms module-wide to keep the suite fast; production runs without this
                                 property and gets the couple-second default. The dedicated drain test drives
                                 the readiness flip directly, not through the pause. -->
                            <configd.shutdown.drainQuietMs>0</configd.shutdown.drainQuietMs>
                        </systemPropertyVariables>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-shade-plugin</artifactId>
                    <version>3.6.0</version>
                </plugin>
                <!-- Pinned for reproducible builds (3.4.2: latest stable for Maven 3.9/JDK 25). -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>3.4.2</version>
                </plugin>
                <!-- Pinned so offline builds can resolve it; offline cache misses break if unpinned. -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-clean-plugin</artifactId>
                    <version>3.2.0</version>
                </plugin>
                <plugin>
                    <groupId>com.github.spotbugs</groupId>
                    <artifactId>spotbugs-maven-plugin</artifactId>
                    <version>4.9.3.0</version>
                    <configuration>
                        <effort>Max</effort>
                        <threshold>Medium</threshold>
                        <failOnError>false</failOnError>
                        <excludeFilterFile>${maven.multiModuleProjectDirectory}/spotbugs-exclude.xml</excludeFilterFile>
                        <xmlOutput>true</xmlOutput>
                        <!-- Java 25 is class-file version 69; ASM 9.9.1 required (9.7.1 doesn't recognize it). -->
                        <fork>true</fork>
                        <spotbugsVersion>4.9.3</spotbugsVersion>
                    </configuration>
                    <dependencies>
                        <dependency>
                            <groupId>org.ow2.asm</groupId>
                            <artifactId>asm</artifactId>
                            <version>9.9.1</version>
                        </dependency>
                        <dependency>
                            <groupId>org.ow2.asm</groupId>
                            <artifactId>asm-analysis</artifactId>
                            <version>9.9.1</version>
                        </dependency>
                        <dependency>
                            <groupId>org.ow2.asm</groupId>
                            <artifactId>asm-commons</artifactId>
                            <version>9.9.1</version>
                        </dependency>
                        <dependency>
                            <groupId>org.ow2.asm</groupId>
                            <artifactId>asm-tree</artifactId>
                            <version>9.9.1</version>
                        </dependency>
                        <dependency>
                            <groupId>org.ow2.asm</groupId>
                            <artifactId>asm-util</artifactId>
                            <version>9.9.1</version>
                        </dependency>
                    </dependencies>
                    <executions>
                        <execution>
                            <id>spotbugs-verify</id>
                            <phase>verify</phase>
                            <goals>
                                <goal>spotbugs</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- Mutation testing + coverage: in a profile so it doesn't slow default builds. -->
        <profile>
            <id>mutation</id>
            <properties>
                <!-- Must be empty so @{argLine} resolves when JaCoCo agent is absent. -->
                <argLine></argLine>
            </properties>
            <build>
                <pluginManagement>
                    <plugins>
                        <plugin>
                            <groupId>org.apache.maven.plugins</groupId>
                            <artifactId>maven-surefire-plugin</artifactId>
                            <configuration>
                                <!-- @{argLine} prepended so the JaCoCo agent is honoured; the enable-preview flag is kept. -->
                                <argLine>@{argLine} --enable-preview</argLine>
                            </configuration>
                        </plugin>
                        <plugin>
                            <groupId>org.jacoco</groupId>
                            <artifactId>jacoco-maven-plugin</artifactId>
                            <version>0.8.14</version>
                            <executions>
                                <execution>
                                    <id>mutation-jacoco-prepare</id>
                                    <goals>
                                        <goal>prepare-agent</goal>
                                    </goals>
                                </execution>
                                <execution>
                                    <id>mutation-jacoco-report</id>
                                    <phase>test</phase>
                                    <goals>
                                        <goal>report</goal>
                                    </goals>
                                </execution>
                            </executions>
                        </plugin>
                        <plugin>
                            <groupId>org.pitest</groupId>
                            <artifactId>pitest-maven</artifactId>
                            <version>1.25.4</version>
                            <dependencies>
                                <dependency>
                                    <groupId>org.pitest</groupId>
                                    <artifactId>pitest-junit5-plugin</artifactId>
                                    <version>1.2.3</version>
                                </dependency>
                            </dependencies>
                            <configuration>
                                <mutators>
                                    <mutator>DEFAULTS</mutator>
                                </mutators>
                                <threads>2</threads>
                                <timeoutConstant>8000</timeoutConstant>
                                <timeoutFactor>1.5</timeoutFactor>
                                <jvmArgs>
                                    <jvmArg>--enable-preview</jvmArg>
                                </jvmArgs>
                                <outputFormats>
                                    <outputFormat>XML</outputFormat>
                                    <outputFormat>HTML</outputFormat>
                                </outputFormats>
                                <timestampedReports>false</timestampedReports>
                                <!-- false: non-target modules are inert (target modules override). -->
                                <failWhenNoMutations>false</failWhenNoMutations>
                            </configuration>
                        </plugin>
                    </plugins>
                </pluginManagement>
                <plugins>
                    <plugin>
                        <groupId>org.jacoco</groupId>
                        <artifactId>jacoco-maven-plugin</artifactId>
                    </plugin>
                    <plugin>
                        <groupId>org.pitest</groupId>
                        <artifactId>pitest-maven</artifactId>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- CVE scan in profile so default builds don't pay for NVD feed updates. -->
        <profile>
            <id>cve-scan</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.owasp</groupId>
                        <artifactId>dependency-check-maven</artifactId>
                        <version>${dependency-check.version}</version>
                        <configuration>
                            <failBuildOnCVSS>7</failBuildOnCVSS>
                            <formats>
                                <format>HTML</format>
                                <format>JSON</format>
                                <format>SARIF</format>
                            </formats>
                            <outputDirectory>${maven.multiModuleProjectDirectory}/target/dependency-check</outputDirectory>
                            <!-- Empty today; path declared so false-positives can be suppressed. -->
                            <suppressionFiles>
                                <suppressionFile>${maven.multiModuleProjectDirectory}/dependency-check-suppressions.xml</suppressionFile>
                            </suppressionFiles>
                            <!-- Disable irrelevant analyzers (pure-Java reactor). -->
                            <assemblyAnalyzerEnabled>false</assemblyAnalyzerEnabled>
                            <nodeAnalyzerEnabled>false</nodeAnalyzerEnabled>
                            <nodeAuditAnalyzerEnabled>false</nodeAuditAnalyzerEnabled>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
